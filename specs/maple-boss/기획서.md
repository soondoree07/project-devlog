# 메이플 보스 파티 기록 — 통합 기획서

**버전:** v0.5 (보스 난이도 모델 — 결정석 난이도별 고정 + 보스 27종 + 보스 설정 페이지) · 디자인 무드 1단계 확정(방문마다 다크 6벌 랜덤) · 공유 백엔드 전환 설계 완료(결정 대기)
**최종 업데이트:** 2026-05-16
**원본 PLAN:** `/mnt/c/Users/박정혁/Downloads/PLAN.md`
**보스 데이터 원본:** `/home/soondoree07/maple-boss/boss_table.md`
**프로젝트 위치:** `/mnt/c/Users/박정혁/Downloads/maple-boss/`
**배포 URL:** https://soondoree07.github.io/maple-boss-party/
**Repo:** https://github.com/soondoree07/maple-boss-party

## 1. 한 줄 요약
메이플스토리 본진 파티의 주간/월간 보스 클리어 + 전리품 분배 내역을 기록·조회하는 1인용 정적 웹사이트. Vanilla HTML + ES Module + localStorage. 빌드 시스템 없음.

## 2. 기술 스택
| 영역 | 선택 |
|---|---|
| 프레임워크 | 없음 (Vanilla HTML/JS/CSS, ES Module) |
| 빌드 시스템 | 없음 |
| 저장소 | 브라우저 localStorage (`maple-boss-v1`) |
| 호스팅 | GitHub Pages (`soondoree07/maple-boss-party` repo, main / root) |
| 폰트 | Pretendard Variable (CDN) |
| 테마 | 다크 |

## 3. 핵심 기능

### 3.1 파티 관리
- 새 파티 생성 (이름 + 닉네임 6칸 그리드, Enter로 다음 칸 이동, 빈 칸은 무시, 1명 이상 입력 시 저장)
- 파티 목록 조회 / 카드 클릭으로 파티 상세 진입
- **파티 카드 우상단 × 버튼**으로 삭제 (호버 시 노출, 모바일은 옅게 항상 노출, confirm으로 기록 N건도 함께 삭제 안내)
- 파티 상세 우상단 "삭제" 버튼도 동일 동작
- **파티원 추가** (v0.4) — 두 곳에서 가능:
  - 메인 파티 카드 멤버 그리드 끝 "+ 추가" 칩 (카드 클릭 이동은 stopPropagation으로 막음)
  - 파티 상세 파티원 strip 끝 "+ 추가" 칩
  - 클릭 시 모달로 닉네임 input → trim/중복 검사 후 `updateParty`로 members 배열에 append

### 3.2 캘린더 뷰
- 월 단위 그리드, **메이플 주차 시작 (목→수)** — 요일 헤더: 목/금/토/일/월/화/수
- 토·일은 빨간색 강조 (`--accent-rose`)
- 오늘 날짜 골드 강조
- ←/→ 버튼으로 월 이동 + **모달 닫고 재렌더해도 보고 있던 월 보존** (`calendarViewByParty` Map)
- 셀 클릭 → 그날 모달
- 셀에 보스별 색상 pill (최대 4개 + `+N` 표시)

### 3.3 진행도 위젯 (v0.4에서 제거)
- v0.3에 있던 '이번 주 현황 / 이번 달 현황' 진행도 위젯은 v0.4에서 제거됨.
  실제 멤버별 수익은 §3.10 / §3.13 earnings 카드 두 개가 대신 노출.
- 결정석 가격 편집 페이지(`#/crystals`) 진입점은 페이지 헤더 actions의 '결정석' 링크로 이동.

### 3.4 회차 기록 흐름

**날짜 모달 (캘린더 셀 클릭)**
- 채널 picker 버튼 그리드 (1 / 20세이상 / 2 / 3 / ... / 39, 8열, 드롭다운 X)
- 회차가 1건이라도 있으면 그 채널로 잠김
- 회차 카드 N개 (보스 / 채널 / 상자 연 사람 / 기본 보상 / **참여자 N인** / 전리품 목록 + 삭제 ×)
- "+ 보스 기록 추가" 버튼 — 채널 선택 전엔 비활성화

**기록 추가 폼**
- 보스 select — **보스 설정에서 '보이기' 켠 보스만**, 이름 가나다순 (수정 모드는 자기 보스가 숨겨졌어도 옵션 유지). 같은 주/달 클리어 제외 가림 없음
- **난이도 select (v0.5 신규)** — 보스 선택 후 노출. 초기값 캐스케이드: ① 이 파티가 그 보스를 마지막으로 기록한 회차의 난이도 → ② 보스 설정 기본 난이도 → ③ 보스의 첫(가장 낮은) 난이도. 매 회차 드롭다운으로 변경 가능. 난이도를 바꾸면 전리품 타일 목록이 그 (보스×난이도) 기준으로 갱신됨. `BossRun.difficulty`로 저장
- **이번 회차 참여자** 토글 그리드 (v0.4 신규) — 파티원 중 이번에 간 사람만 토글. 후보는 파티 생성 시 등록된 멤버로 한정. **같은 날 직전 회차의 멤버를 자동 미리 채움 / 직전 회차가 없으면 파티 전체.** 회차 참여자 변경 시 상자 연 사람·기본 보상·전리품 taker의 select 옵션도 그 멤버로만 좁아짐.
- 상자 연 사람 select / 기본 보상 select (form-row 2열) — 직전 회차의 값으로 자동 미리 채움
- 전리품 입력은 **타일 그리드** (이미지 + 이름, 토글 선택). select 폐기.
- 선택된 전리품마다 [이름 라벨] [먹은 사람] [가격] **[분배 체크박스]** [×] 행으로 자동 추가
  - **분배 ON (디폴트):** 가격을 회차 참여자 N명에게 1/N씩 분배. taker select는 비활성 + '분배: 전원 N등분' 표기.
  - **분배 OFF:** taker 1명에게 가격 전액. taker 필수.
- **저장 필수: 보스 + 난이도 + 회차 참여자 1명 이상 + 상자 연 사람.** 기본 보상 / 전리품은 비워도 OK.
- 전리품 칸 처리: 표에 적힌 (보스×난이도)는 표 값 / 빈칸이면 **기존 9보스는 v0.4 보스별 전리품(`LEGACY_LOOT`)을 이름 매칭해 채움**, 신규 18보스만 "전리품 없이 결정석만" 안내 (사용자 결정)

### 3.5 보스 설정 · 결정석 페이지 (`#/crystals`) — v0.5 전면 개편
- 페이지 헤더 '보스 설정' 링크로 진입
- **결정석 가격은 보스×난이도별 고정값** (`boss_table.md` 기반, 자유 입력/`crystalOverrides` 폐기)
- 보스마다 **가로로 긴 행 1개**, 보스 이름 **가나다순**:
  `[☑ 보이기] [보스명] [주간/월간] [기본 난이도 ▼]` + 난이도별 `[난이도 · 결정석 가격 · 전리품 종류]` 조회
- **보이기 체크박스** — 끄면 회차 폼 보스 드롭다운에서 숨김(기존 기록·캘린더·수익엔 그대로 노출). `bossSettings.visible`
- **기본 난이도 select** — 회차 폼 난이도 초기값 후보. `bossSettings.defaults`
- 난이도별 결정석/전리품은 조회 전용(고정값)
- 페이지 하단 저장 버튼 1개 → `bossSettings`에 visible/defaults 일괄 저장
- (보스 표시 순서 커스텀은 추후 — 현재 가나다순)

### 3.6 백업/복원
- `↓ 백업` 버튼 — 전체 데이터 JSON 다운로드 (`maple-boss-backup-YYYY-MM-DD.json`)
- `↑ 복원` 버튼 — JSON 업로드, confirm 후 덮어쓰기
- `crystalOverrides` / `reservations`도 백업/복원에 포함

### 3.7 미래 날짜 예약
- 캘린더에서 **미래 날짜 클릭** → 예약 모달
- `<input type="time">`으로 시간 입력 → "+ 추가"
- **하루에 1예약**만 (1건이라도 있으면 입력 폼 숨김 / 카드 + × 삭제만 노출)
- 캘린더 셀에는 골드 점선 보더 pill로 시간(`HH:MM`) 표시. 셀에 `has-reservation` 클래스 부여

### 3.8 월별 누적 사이드바
- 파티 상세 우측 sticky 320px 컬럼
- 오늘 기준 **이번 달 / 1달 전 / 2달 전** 카드 3개
- 헤더: `YYYY년 M월` + 우측에 **인당 총 결정석** (그 달 weekly+monthly 결정석 합 ÷ 인원, 0이면 `0억`, override 반영)
- 본문: 회차 전리품을 **최근 날짜순**으로 평탄화 → `[이미지][이름(유니크 도미넌트 컬러)][나누기 전 가격]` 행
- 회차 0건이면 "기록 없음"

### 3.9 채널 룰렛 사이드 카드
- 파티 상세 좌측 sticky 220px 컬럼 (룰렛 + 사다리타기 wrapper `side-left`)
- 슬롯 윈도우 + 시안/보라 그라데이션 "🎰 뽑기" 버튼
- 버튼 누르면 1.1초 동안 굴러가며 채널 후보를 스피닝 → eased tick interval(35ms→180ms) → CHANNELS 1개로 멈춤
- `roulette-shake` (스피닝 중) / `roulette-pop` (착지 시) 키프레임 애니메이션

### 3.10 파티원별 주간 수익 (v0.4 신규, 메인 컬럼 최상단)
- 메이플 주차(목 0시 ~ 수 23:59) 기준 멤버별 수익을 합산
- 각 멤버의 수익 = Σ(그 주에 본인이 회차 참여자에 포함된 회차들에 대해)
  - **결정석:** `getEffectiveCrystal(boss) / 회차 인원수`
  - **분배 전리품:** `lt.price / 회차 인원수` (회차 참여자 1몫)
  - **단독 전리품:** `lt.price` 전액 (taker가 본인일 때만)
- 총수익 desc 정렬, aqua→purple 그라디언트 progress bar로 비중 표시
- 이번 주 회차 0건이면 "이번 주 회차 기록이 없어요"

### 3.13 이번 달 전체 수익 (v0.4 신규, 주간 수익 바로 아래)
- 이번 달(1일 ~ 말일) 기준. §3.10과 동일한 합산 식
- 카드 헤더 우측에 **'파티 전체' 누적** 합계 표기 (그 달 모든 멤버 수익의 합)
- 본문은 §3.10과 동일한 멤버별 progress bar 행

### 3.11 사다리타기 카드 (v0.4 신규)
- 룰렛 카드 바로 아래 (좌측 사이드 컬럼)
- **2단계 UX:**
  1. **입력 단계 (가림)** — 인원수 input(2 ~ 파티 총원, 디폴트 2) + 이름 input N개. SVG 사다리는 블러 처리 + 🎴 cover로 가려짐. "🎲 뽑기" 버튼.
  2. **공개 단계** — 뽑기 누르면 새 사다리(랜덤 가로줄) + 결과(O 1명, X N-1명) 배치 + 공개. 이름 input들이 **클릭 가능한 색상 칩**으로 전환.
- **이름 칩 클릭** — 그 사람의 path를 색상 stroke로 그려가며(stroke-dashoffset 애니메이션) 점이 따라가는 모습이 보인다(1.5초, cubic ease). 도착 컬럼의 결과 셀이 outline + pop으로 강조.
- 다른 이름 클릭 시 이전 trace 제거 후 새로 그림
- 칩 색: aqua / purple / rose / gold / green / red 순환
- 카드 하단에 🏆 당첨자 안내 + "👆 이름을 눌러서 사다리를 따라가보세요" 힌트
- "🎲 다시 뽑기"로 입력 단계로 복귀
- 1인 파티는 "2명 이상부터 가능해요" 안내

### 3.12 회차 기록 수정 (v0.4 신규)
- 날짜 모달의 회차 카드에 ✎ 수정 버튼 (× 옆)
- 클릭 시 기록 추가 폼 재사용. 모든 필드(보스/회차 참여자/상자 연 사람/기본 보상/전리품 + taker + 가격 + 분배) prefill
- 보스 select은 같은 주/달 클리어 보스를 가리되 **자기 회차의 보스는 옵션에 남김** (그대로 유지하거나 다른 보스로 바꾸기 위해)
- 보스를 바꾸면 lootEntries 비우는 가드. 같은 보스로 두면 prefill 그대로 유지
- 저장은 `Storage.updateRun(existingRun.id, payload)` — 같은 회차 ID 유지

## 4. 데이터 모델

```js
{
  parties: [
    { id, name, members: [닉네임...], createdAt }
  ],
  bossRuns: [
    {
      id, partyId, date,
      boss,                          // 'seren' | ... | 'blackmage'
      channel,                       // "1" | "20세이상" | "2" ... "39"
      opener,                        // 상자 연 사람 (필수)
      baseReward,                    // 기본 보상 받은 사람 (선택)
      difficulty,                    // v0.5: 'easy'|'normal'|'hard'|'chaos'|'extreme'
                                     //   legacy 미정의 → resolveDifficultyKey fallback
      memberSnapshot: [닉네임...],   // v0.4부터 회차 실제 참여자(파티 일부 가능)
      loot: [{ item, taker, price, shared }]
        // price 단위: 억 (number | null)
        // shared: true=회차 N명 분배 / false=taker 전액. 미정의(legacy)는 단독 해석.
    }
  ],
  reservations: [                    // 미래 날짜 보스 일정 (하루 1건)
    { id, partyId, date, time, createdAt }   // time: "HH:MM"
  ],
  bossSettings: {                    // v0.5 (구 crystalOverrides 폐기)
    visible:  { [bossId]: false },   // false면 회차 폼 보스 드롭다운에서 숨김
    defaults: { [bossId]: difficultyKey }  // 보스별 기본 난이도
  }
}
```
> 결정석 가격은 더 이상 데이터에 저장하지 않는다. `data.js`의 보스×난이도 고정값(`difficulties[].crystal`, 단위 억)에서 `getEffectiveCrystal(bossId, difficultyKey)`로 조회.

## 5. 도메인 상수

### 5.1 보스 (v0.5 — 27종, 난이도별 결정석 고정)

**난이도 키:** `easy`(이지) `normal`(노멀) `hard`(하드) `chaos`(카오스) `extreme`(익스트림). data.js에서 보스마다 `difficulties: [{ key, crystal(억), loot }]` (난이도 오름차순).

**기존 9보스 ID 보존** (풀네임 매핑 — 기존 BossRun 호환):
seren=선택받은 세렌 · kalos=감시자 칼로스 · adversary=최초의 대적자 · kaling=카링 · lotus=찬란한 흉성 · limbo=림보 · baldrix=발드릭스 · jupiter=유피테르 · blackmage=검은 마법사(월간)

**신규 18보스:** gas(가디언 엔젤 슬라임) · damien(데미안) · dusk(더스크) · dunkel(듄켈) · lucid(루시드) · magnus(매그너스) · vonbon(반반) · vellum(벨룸) · bloodyqueen(블러디퀸) · suu(스우) · cygnus(시그너스) · will(윌) · zakum(자쿰) · jinhilla(진 힐라) · papulatus(파풀라투스) · pierre(피에르) · pinkbean(핑크빈) · hilla(힐라) — 검은 마법사 외 전부 weekly

> 결정석 가격·난이도·전리품 단일 출처: `/home/soondoree07/maple-boss/boss_table.md` → `data.js`. 사용자 자유 입력(`crystalOverrides`) 폐기. node 검증 스크립트로 표 전수 대조 완료.
> 보스 등장 유무·기본 난이도만 `#/crystals`(보스 설정 페이지)에서 사용자 설정 → `bossSettings`.

### 5.2 채널 (40개)
인게임 채널 선택창 순서: `1 → 20세이상 → 2 → 3 → ... → 39`

### 5.3 전리품 색상
- `hammer` `#D4A056` — 해머 5종
- `purple` `#9B5DE5` — 퍼플코어 8종
- `unique` — **항목별 도미넌트 컬러** (`LOOT_NAME_COLOR`):
  - 황홀한 악몽 `#FE9500`
  - 근원의 속삭임 `#A500A6`
  - 죽음의 맹세 `#00FEFD`
  - 불멸의 유산 `#FFA700`
  - 창세의 뱃지 `#FB0400`
  - 오만의 원죄 `#C9B58E`
- `default` `#1A1A1A` — 그 외

### 5.4 보스별 전리품
- 세렌: 해머(얼굴장식), 미트라의 분노, 영달포 + 공통
- 칼로스: 해머(눈장식), 에테상자, 연마석, 영달포 + 공통
- 대적자: 해머(훈장), 불멸의 유산, 에테상자, 연마석, 영달포 + 공통
- 카링/흉성/림보/발드: 해머·유니크·박스·신마석 + 퍼플코어 7종 + 공통
  - 카링: 해머(귀고리), 에테상자, 신마석
  - 흉성: 황홀한 악몽, 에테상자, 신마석
  - 림보: 근원의 속삭임, 장신망상자, 신마석
  - 발드: 죽음의 맹세, 장신망상자, 신마석
- 유피테르: **오만의 원죄** + 공통 (보스는 비활성 상태)
- 검마: 해머(벨트), 창세의 뱃지 + 공통

### 5.5 전리품 이미지 (`png/`)
27개 항목 + 오만의 원죄 모두 매칭. 신마석/연마석/장신망상자만 `.webp`, 나머지 `.png`.

## 6. 라우팅
| 경로 | 화면 |
|---|---|
| `#/` | 메인 (파티 목록) |
| `#/party/:id` | 파티 상세 (룰렛+사다리 / 진행도+캘린더+주간수익 / 월별 사이드) |
| `#/crystals` | 결정석 가격 수정 |

## 7. 모듈 구조
```
maple-boss/
├── index.html
├── background/                     (랜덤 배경 6장)
├── css/style.css
├── png/                            (전리품 + 결정석 이미지)
└── js/
    ├── app.js          # 해시 라우팅 + 파티 상세 + 랜덤 배경 주입 + side-left wrapper
    ├── data.js         # 보스/채널/전리품 상수 + LOOT_IMAGE/LOOT_NAME_COLOR + getEffectiveCrystal
    ├── storage.js      # localStorage CRUD + crystalOverrides + reservations
    ├── utils.js        # 날짜/메소 포맷/DOM 헬퍼
    ├── party.js        # 메인 (파티 목록 + 6칸 모달 + 카드 삭제)
    ├── progress.js     # 주간/월간 위젯 (회차별 1/N 합산, 결정석 클릭 → #/crystals)
    ├── calendar.js     # 캘린더 (목 시작, viewedMonth 보존, 예약 pill)
    ├── record.js       # 날짜 모달 + 기록 폼 (회차 참여자 picker, 분배 체크) + 예약 폼
    ├── monthly.js      # 월별 누적 사이드바 (이번/-1/-2, 회차별 1/N)
    ├── earnings.js     # 파티원별 주간 수익 카드 (v0.4 신규)
    ├── roulette.js     # 채널 룰렛 사이드 카드
    ├── ladder.js       # 사다리타기 카드 (v0.4 신규)
    ├── crystals.js     # 결정석 가격 편집 페이지
    └── backup.js       # JSON 내보내기/불러오기
```

## 7.1 파티 상세 레이아웃 (3컬럼 grid)
```
┌────────────────────── party-members-strip (양쪽에 걸침) ──────────────────────┐
│  [룰렛 220px]   │   [메인 1fr — 진행도 + 캘린더]   │  [월별 사이드 320px]    │
│  sticky         │                                  │  sticky                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```
- `#app max-width: 1180px` (양옆/위아래로 배경 사진 노출)
- `1080px` 이하에서는 1컬럼으로 stack (사이드들이 캘린더 아래로)

## 7.2 디자인 / 테마
- **배경:** 페이지 로드(방문)마다 무드 6벌(1·2·3·4·6·8) 중 1벌을 랜덤 적용 — `index.html <head>` 인라인 스크립트가 `style.css` 뒤에 `css/themes/mood-N.css` `:root` 오버라이드 `<link>`를 append. 무드별 **단색 배경**(구 `background/` 이미지·overlay·`applyRandomBackground` 폐기)
- **팔레트:** 시안/aqua + 보라
  - `--accent-gold #4ee5f5` (구 골드 자리. CSS 토큰명만 유지)
  - `--accent-purple #a78bff`, `--accent-rose #ff7eb6`
  - `--accent-aqua #4ee5f5` (룰렛 등 별칭)
- **카드:** `rgba` 반투명 + `backdrop-filter: blur(10px) saturate(125%)`로 사진 위 가독성

> **CSS variable url() 함정:** variable의 `url()`은 *그 variable이 사용되는 stylesheet 위치* 기준으로 풀림. `/css/style.css`에서 `var(--bg-image)`를 쓰면 `css/background/...`로 잘못 풀려 404. **CSS 디폴트도 JS override도 모두 `../background/` prefix 통일.**

#### 무드 테마 시스템 (2026-05-16~ , in-repo CSS-only)
- `style.css`는 **완전 토큰 기반**으로 리팩터됨 — 색/블러/배경이 모두 `:root` 토큰. 하드코딩 잔존은 의도된 `--shadow-*`(테마 오버라이드 가능)와 사다리 가림용 `blur(2px)`(게임 연출, 배경블러 아님)뿐
- 무드 변경 = **`:root` 토큰만 교체**. 구조/클래스/JS DOM은 절대 안 건드림 → "구조 그대로, 무드만" 보장
- **확정(2026-05-16):** 단일 무드 고정 대신 **방문(로드)마다 무드 6벌 중 1벌 랜덤**. `index.html <head>` 인라인 스크립트(`var moods=[1,2,3,4,6,8]`)가 `style.css` 뒤에 `css/themes/mood-N.css` `<link>`를 동기 append → FOUC 없음. 운영 무드 6벌(전부 다크) = **1 Midnight Slate · 2 Abyss Teal · 3 Crimson Noir · 4 Neon Synth · 6 Royal Plum · 8 Carbon Amber**. 임시 전환기(`js/theme-switch.js`)·미사용 무드 5(Graphite Mono)·7(Espresso Copper)·`style.pre-redesign.css` 백업 삭제, app.js `applyRandomBackground`/`BACKGROUNDS` 제거
- 핵심 토큰: 배경(`--bg-*`/`--page-bg`), 강조(`--accent-*`), 강조 위 글자(`--on-accent`=테마 강조색 위 / `--on-boss`=JS 보스색 위 항상 어둡게), 데코 알파 RGB트리플(`--glow-rgb`/`--shade-rgb`/`--hairline-rgb`/`--ink-rgb`/`--danger-rgb`/`--ladder-grad-rgb`), 필터(`--card-filter`/`--overlay-filter`, 테마에서 `none`)
- 되돌리기: git 태그 `pre-redesign-2026-05-16` (토큰화 이전 style.css). `css/style.pre-redesign.css` 백업 파일은 1단계 확정과 함께 삭제됨

## 8. 개발 히스토리

### 2026-05-16 — v0.5 보스 난이도 모델 전면 도입 (결정석 난이도별 고정 + 보스 27종 + 보스 설정 페이지)
- **결정석 = 보스×난이도별 고정값** — 사용자 자유 입력(`crystalOverrides`) 폐기. `boss_table.md`(사용자 제공)의 메소를 억 단위로 변환해 `data.js`에 박제. node 스크립트로 표와 전수 대조(ALL PASS, 27보스)
- **보스 27종** — 기존 9 + 미등록 18. 기존 9보스는 ID 보존(seren/kalos/lotus/baldrix/adversary/kaling/limbo/jupiter/blackmage)하고 풀네임으로 매핑 → 기존 BossRun 자동 호환
- **data.js 재작성** — 보스에 `difficulties:[{key,crystal,loot}]`. 난이도 라벨/정렬 헬퍼 + `getEffectiveCrystal(boss,diff)`/`getBossLoot`/`resolveDifficultyKey`/`getVisibleBosses`/`bossesByName`/`getLootGroup` 신설. 전리품 그룹은 아이템 이름 기반으로 단순화 → `BOSS_LOOT`/`getLootDef`/`getEnabledBosses` 제거
- **전리품 병합 규칙** — 표에 적힌 (보스,난이도)는 표 값 / 빈칸이면 **기존 9보스(seren/kalos/adversary/kaling/lotus/limbo/baldrix/jupiter/blackmage)는 v0.4 보스별 전리품을 이름 매칭한 `LEGACY_LOOT` 맵으로 채움**, 신규 18보스 빈칸만 결정석만 (사용자 결정). 초기 구현 때 세렌만 적용한 버그를 같은 날 수정(`3c23f70`)
- **커포 → 커포링 통일** — `LOOT_GROUP`/`LOOT_IMAGE` 키 변경, 이미지 `png/커포.png` 재사용, 레거시 `커포` 기록 호환
- **storage.js** — `crystalOverrides` 폐기, `bossSettings:{visible,defaults}` getter/setter + readRaw/import 정규화. 구버전 백업의 crystalOverrides는 무시
- **crystals.js → 보스 설정 페이지** — 보스당 가로행 `[☑ 보이기][보스명][기본난이도▼]` + 난이도별 결정석·전리품 조회, 가나다순. 저장 시 bossSettings 반영
- **record.js** — 보스 select(보이는 보스만,가나다순) 후 난이도 select 노출. 난이도 초기값 캐스케이드(직전 회차>보스 기본설정>첫 난이도). 전리품 타일=(보스×난이도). `BossRun.difficulty` 저장. 회차 카드 난이도 칩
- **earnings.js/monthly.js** — 결정석을 `resolveDifficultyKey(boss,run.difficulty,defaults)` 기준 합산, 난이도 없는 legacy 회차는 기본설정→첫난이도 fallback
- **CSS** — 보스 설정 가로행(`.boss-setting-card`/`.bsd-row`)·`.run-difficulty` 칩·`.form-hint`, 구 `.crystal-card*` 제거
- **검증** — 전 JS `node --check` 통과 / data.js 표 전수 대조 ALL PASS / 로컬 서버 주요 자산 200(한글 png 포함). 커밋 `a16ed37` GitHub Pages 푸시 → 사용자 브라우저 검증 대기
- **파티 비밀번호 게이트(디자인 작업 전 선행)** — `Party.pw`(SHA-256, 선택). 새 파티 모달 비번칸, `#/party/:id` 진입 게이트(메모리 `unlockedParties`, 재접속 재입력), 상세 헤더 '비밀번호' 설정/변경/해제 버튼. ⚠️ localStorage 기반이라 진짜 보안 아님 — 실보안은 백엔드 필요(아래 2단계 설계)

### 2026-05-16 (후속) — 디자인 무드 개편 1단계(in-repo CSS-only) + 공유 백엔드 전환 설계(2단계 문서)
- **방식 확정** — 외부 도구 없음. 클로드 코드가 이 레포에서 직접 CSS만 작성 → 구조/클래스/JS 100% 보존
- **style.css 토큰화 리팩터** — 하드코딩 색/블러 약16곳(`#1a1102`·`#0b1626`·`rgba(78,229,245…)`·`rgba(5,11,24…)`·blur 등)을 의미 토큰(`--on-accent`/`--on-boss`/`--btn-grad-text`/`--glow-rgb`/`--shade-rgb`/`--hairline-rgb`/`--ink-rgb`/`--danger-rgb`/`--ladder-grad-rgb`/`--card-filter`/`--overlay-filter`/`--page-bg`)으로 치환. **값 1:1 동일 → 현재 렌더 무변화**(검증: 잔여 하드코딩 0, 미정의 var 0, brace 균형, 자산 200). 이후 무드는 `:root`만 덮어쓰면 됨
- **무드 8벌** `css/themes/mood-1..8.css` — 라이트4·다크4 (1 Midnight Slate, 2 Pure Paper, 3 Warm Sand, 4 Forest Night, 5 Mono Ink, 6 Royal Plum, 7 Nordic Frost, 8 Carbon Amber). 각 파일은 순수 `:root` 오버라이드(완성형). 단색 배경·블러 제거·WCAG AA 대비·반응형 유지. 보스/전리품 기능색은 JS 인라인이라 그대로 식별성 유지. `run-boss-badge`는 JS 보스색 위라 `--on-boss`로 분리(항상 어둡게)
- **임시 무드 전환기** — `js/theme-switch.js` + `index.html` 1줄. 우하단 fixed select, `style.css` 뒤에 `<link>`를 얹어 :root만 교체, 선택 localStorage 유지. **무드 확정 후 `js/theme-switch.js`·`css/themes/`·index.html 1줄 삭제 + `applyRandomBackground()` 비활성 + 선택 무드를 style.css로 확정** 예정. 커밋 `3386a9f`(GitHub Pages 푸시 → 사용자가 8무드 비교·선택)
- **공유 백엔드 전환 설계(2단계, 문서)** — `BACKEND_PLAN.md`. localStorage는 브라우저별 격리라 다인 공유 불가 → DB 필요. 스택비교 후 **Supabase 추천**(관계형이라 데이터모델 재설계 0, Realtime로 "남이 수정→모두 즉시 반영", 서버 불필요로 현 정적배포 유지, Auth/RLS로 비번 승격 가능). 데이터모델(parties/boss_runs/reservations/boss_settings 테이블), `storage.js`는 **인메모리 캐시+Realtime(방식 2)** 권장(읽기 동기 유지 → 화면 코드 거의 그대로, app.js 2곳만 수정), 1회 마이그레이션(기존 backup JSON→upsert 멱등), 비번을 서버 verify RPC + RLS로 승격. 작업 약 3~4.5일. 사용자 결정 4개(스택/공개범위/boss_settings 전역여부/호스팅) 후 P1 착수. 커밋 `80b9582`

### 2026-05-16 (후속2) — 디자인 무드 개편 1단계 확정 (방문마다 6벌 랜덤)
- **무드 세트 재구성** — 라이트 무드 전부 폐기, 다크 8벌로(1·6·8 유지 + 신규 다크 5: 2 Abyss Teal·3 Crimson Noir·4 Neon Synth·5 Graphite Mono·7 Espresso Copper). 커밋 `8edb39d`
- **2단계 설계 코드검증** — Explore 2종으로 `BACKEND_PLAN.md`가 실제 코드와 1:1 일치 확인. `Storage.*` 호출 ~95%가 동기 렌더 경로 → 인메모리+Realtime(방식 2) 권장 확정
- **1단계 확정 방식 = 단일 무드 고정이 아니라 방문(로드)마다 6벌 랜덤** (사용자 결정). `index.html <head>`에 인라인 스크립트(`moods=[1,2,3,4,6,8]` 중 1벌 → `style.css` 뒤 `css/themes/mood-N.css <link>` 동기 append). 구조/클래스/JS DOM 불변·FOUC 없음
- **정리** — app.js `applyRandomBackground`/`BACKGROUNDS` 제거(배경=무드 단색), 임시 전환기 `js/theme-switch.js`·index 임시 script 삭제, 미사용 무드 5·7 + `css/style.pre-redesign.css` 백업 삭제(되돌리기는 git 태그 `pre-redesign-2026-05-16`)
- **검증** — 로컬 서버 index + 무드 6벌 + style.css + app.js 전부 200, 댕글링 참조 0. 커밋 `449dd44` GitHub Pages 푸시. **1단계 종료** → 다음은 2단계 결정 4개
### 2026-05-07 — v0.4 회차별 참여자 + 전리품 분배 + 파티원별 주간 수익 + 사다리타기 SVG 시각화 + 회차 수정
- **데이터 모델 변경** — `BossRun.memberSnapshot`은 그대로 두되 의미를 "회차 실제 참여자"로 (기존엔 그 시점 파티원 전체였으므로 자동 호환). `LootEntry.shared` 플래그 신규(true=분배 / false=단독). 기존 entry는 shared 미정의 → 단독으로 해석해 기존 동작 보존
- **회차 참여자 picker** (record.js 폼) — 파티원 토글 그리드. 후보는 파티 생성 시 등록된 멤버만. 같은 날 직전 회차의 참여자를 자동 미리 채움 / 직전 회차 없으면 파티 전체. 참여자 변경 시 opener·baseReward·전리품 taker select 옵션도 그 멤버로 좁아짐
- **전리품 분배 체크박스** — 행에 분배 체크박스 추가. 디폴트 ON. ON일 땐 taker select 비활성화 + '분배: 전원 N등분' placeholder. OFF면 taker 1명 필수
- **회차 카드(조회) 표시 개선** — 참여자 명단 행 추가 (이름 + N인). 분배 ON 전리품은 taker 자리에 '분배 ÷N' aqua 색
- **결정석 분배** — Σ(getEffectiveCrystal/회차 인원). progress.js·monthly.js 둘 다 회차별 1/N 합산으로 변경 (v0.3까지는 합계/파티 전체 인원)
- **earnings.js 신설** — 파티원별 이번 주(목~수) 수익 카드. 결정석 1/N + 분배 전리품 1/N + 단독 전리품 전액 합산. 총수익 desc, aqua→purple 그라디언트 progress bar로 비중 시각화. 캘린더 바로 밑
- **ladder.js — 1차 단순 셔플 → 2차 SVG 시각화로 전면 재작성**. makeRungs(인접 가로줄 금지 룰) / traceLadder(corner points 시퀀스) / buildLadderSvg(viewBox 100×192, spine + rung) / animateDots(rAF + 누적 segment 길이 보간 + cubic ease 1.7초). 점 N개가 색상별로(aqua/purple/rose/gold/green/red) 동시에 내려가며 가로줄에서 휘어짐 → 결과 셀 pop + '🏆 X 당첨!'
- **회차 기록 수정** — record.js openRecordForm에 existingRun 파라미터 추가. 수정 모드에서 boss/members/opener/baseReward/loot(taker+price+shared) 모두 prefill. 보스 select은 자기 보스도 옵션에 남김 (clearedBossIds 계산에서 자기 회차 filter). 저장 시 updateRun(existingRun.id). 회차 카드에 ✎ 수정 버튼(aqua hover) + ×(rose hover) 나란히
- **side-left wrapper** — 룰렛+사다리를 좌측 sticky 컬럼 안에 함께 배치
- **CSS 추가** — member-toggle-grid / loot-shared-check / loot-taker-shared / earnings-section + bar / ladder-board (SVG spine/rung/dot) / result-win/lose/pending + pop / side-left / run-card-actions + icon-btn-edit. 모바일 540px 이하 loot-form-row를 3행 grid로 재배치(분배 체크 area 추가)

### 2026-05-04 — MVP + 월별 사이드 / 예약 / 룰렛 / aqua 테마 / GitHub Pages 배포
- 9개 모듈 + index.html + style.css 처음부터 작성 (PLAN.md 명세 기반, reference zip 참고)
- **파티 모달:** 닉네임 1칸 입력 → 6칸 고정 그리드 (Enter로 다음 칸)
- **파티 카드 삭제:** 우상단 × 버튼
- **`baseReward` 필드 신설** — "먹은 사람"의 의미를 "기본 보상 받은 사람"으로 명확화
- **채널 picker 버튼 그리드** — 드롭다운 폐기, 캘린더 날짜 모달에서 1·20세이상·2~39 버튼으로 선택
- **같은 날 채널 잠금 + 같은 주/달 클리어 보스 드롭다운 자동 제외**
- **전리품 입력 타일 그리드** — select 폐기 (글자색이 dark 배경과 합쳐지던 버그 동시 해결), 이미지 + 이름 토글
- **이미지 27+1종 sync** — 사용자가 `/home/soondoree07/maple-boss/png/`에 추가, 프로젝트 안 `png/`로 cp
- **유니크 6종 글자색** — Pillow로 도미넌트 컬러 추출 후 `LOOT_NAME_COLOR` 매핑
- **유피테르에 "오만의 원죄"(unique) 추가** (보스는 여전히 `enabled: false`)
- **결정석 가격 편집 페이지 신설** (`#/crystals`) — 주간 2열 카드 + 월간 별도 섹션 + 페이지 저장 버튼 1개
- **분배액 버그 fix** — 주간 합계가 검마(월간)나 비활성 보스도 더하던 문제 → cycle 가드 + override 적용
- **월간 영역 결정석 + 1인 분배액 추가** — 검마 결정석 ÷ 인원
- **트리거 키워드 `메이플보스` / `maple-boss` 등록** — `/메이플보스` 슬래시 커맨드 + 글로벌 CLAUDE.md 반영
- **결정석 가격 편집 저장 UX 개선** — alert 제거 후 `history.back()`으로 즉시 이전 화면
- **결정석 아이콘 22→30px + summary 수직 정렬** baseline → center
- **분배액 0건 표기** `—` → `0억`
- **월별 누적 사이드바** 신설 (`monthly.js`) — 이번/-1/-2 카드 3개, 인당 총 결정석 + 전리품 행
- **파티원 strip을 grid 양쪽에 걸쳐서** 좌우 첫 카드가 같은 y에서 시작
- **미래 날짜 예약** (`storage.reservations`) — 시간만 입력, 하루 1건 제한, 캘린더 셀에 골드 점선 pill 표시
- **배경 이미지 6장 + aqua 팔레트 전환** — 페이지 로드마다 랜덤. 골드 → 시안. 카드 backdrop-blur
- **CSS variable url() base 함정 학습** — `../background/` prefix 통일로 해결
- **z-index 스태킹 함정** — html 직접 background로 해결 (body::before -2 깔면 html bg에 가려짐)
- **GitHub Pages 배포 완료** — `soondoree07/maple-boss-party` repo, https://soondoree07.github.io/maple-boss-party/
- **채널 룰렛 사이드 카드** (`roulette.js`) — 좌측 220px, 슬롯 스피너 + 시안/보라 그라데이션 버튼
- **회차 카드 전리품 행에도 이미지 표시** — 캘린더 셀 클릭 시 모달의 loot-list에 `loot-img` 24×24 추가

## 9. 알려진 이슈 / TODO

- **유피테르 활성화 미정** — 오만의 원죄는 추가됐으나 `enabled: false`
- **해머/박스/석재 fallback 표시** — 모두 이미지 매칭됐으니 fallback 안 뜸. 단, 기본 그룹 색은 dark라서 시각 미흡 → 필요시 색 지정
- **GitHub Pages 한글 파일명 서빙** — 첫 배포 후 배경/전리품 이미지 서빙 정상 여부 사용자 확인 대기 (안 되면 영문 alias rename)

## 10. 추후 개발 (Phase 2+)
- 유피테르 정식 활성화 (`enabled: true` 한 줄)
- 보스별 통계 (월간 누적 결정석, 멤버별 누적 가치)
- 메소 자동 정산 ("A가 B에게 X억 보내야 함")
- 보스 난이도 기록
- 다중 디바이스 동기화 (Firebase/Gist)
