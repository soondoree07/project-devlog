# 포챔스 (Pochams Battle) 기획서

> **버전:** 9.6  
> **최종 업데이트:** 2026-06-24  
> **⚠️ 2026-06-20 대규모 개편:** 커뮤니티(샘플·팀 공유)·로그인·상세통계를 제거하고 **도감 · 대미지 계산기 · 주간 사용률 Top10 · 버그신고 · 패치노트** 중심의 레퍼런스 도구로 전환. 아래 본문의 샘플/팀/통계/i18n·유저 관련 섹션은 개편 이전 기준이며 추후 정리 예정.  
> **배포 URL:** https://pochams.com  
> **구 URL:** https://pochams-battle.vercel.app → pochams.com으로 301 리다이렉트  
> **레포지토리:** github.com/soondoree07/pochams_project

---

## 1. 프로젝트 개요

포챔스 배틀은 **포켓몬 챔피언스** 게임 플레이어를 위한 종합 전략 플랫폼이다.  
샘플(세부 육성 구성) 공유, 팀 빌딩, 대미지 계산, 사용률 통계 기능을 하나의 사이트에서 제공한다.

### 핵심 가치
- 비로그인으로도 샘플 등록·팀 빌딩 가능 (익명 참여 허용)
- 커뮤니티 좋아요 기반 인기 샘플 큐레이션
- 정확한 대미지 계산기 (Gen 9 규칙 적용)

---

## 2. Pokenova와의 관계

### 2-1. 데이터 공유 구조

```
pokenova_project/data/          pochams_project (DB)
─────────────────────────       ──────────────────────────
quiz_pokemon.json       ──seed──▶  pokemon_data 테이블
quiz_moves.json         ──seed──▶  move_data 테이블
quiz_items.json         ──seed──▶  item_data 테이블
quiz_abilities.json     ──seed──▶  ability_data 테이블
learnsets.json          ──seed──▶  pokemon_learnset 테이블
```

- **pokenova가 원천 데이터 관리자.** 포켓몬·기술·도구·특성·배우는기술 데이터는 pokenova 레포의 JSON 파일에서 관리한다.
- **pochams는 그 데이터를 DB에 seed해서 사용.** `npm run seed` 한 번으로 모든 마스터 데이터가 갱신된다.
- 두 프로젝트는 **Neon PostgreSQL 하나를 공유**한다. DB 수정은 pokenova JSON → seed 재실행 순서로만 한다.
- **동기화 브릿지 `npm run sync` (2026-06-20):** JSON 한 곳 수정 후 한 명령으로 ①champions_learnsets 재생성 ②포챔스 DB seed ③포케노바 data 커밋까지 수행. `--push`를 붙이면 포케노바 배포(git push)까지. 포챔스는 정적 사이트가 아니라 DB에서 읽으므로 seed 즉시 반영(재배포 불필요), 포케노바는 정적 사이트라 push 후 재배포.

### 2-2. 역할 분리

| 항목 | pokenova | pochams |
|---|---|---|
| 역할 | 포켓몬 도감 + 퀴즈 플랫폼 | 팀 빌딩 + 샘플 공유 플랫폼 |
| 마스터 데이터 관리 | ✅ JSON 파일로 관리 | ❌ (읽기만) |
| 유저 계정 | ❌ | ✅ NextAuth |
| 커뮤니티 기능 | ❌ | ✅ 좋아요, 팀, 샘플 |
| 계산기 | ❌ | ✅ 대미지 계산기 |
| 배포 플랫폼 | Vercel (pokenova.pochams.com) | Vercel (pochams.com) |

### 2-3. 챔피언스 전용 필터

pokenova의 기술·도구 데이터에는 `in_champions` 플래그가 있다.  
포켓몬의 챔피언스 노출 여부는 **`pokenova_project/data/champions_pokemon_ids.json` 단일 원천**에서 정한다(baseIds + 원종 제외·특정 폼만 포함하는 altOnlyEns/megaOnlyEns). `seed.ts`와 `regen_champions_learnsets.py`가 이 파일을 함께 읽는다.  
pochams는 이 플래그/목록이 `true`·포함인 데이터만 사용해 포켓몬 챔피언스 범위로 제한한다.  
단, 관련 포켓몬(파트너/카운터) 태그 입력에는 메가진화폼도 포함된다(`includeMega=true`).

---

## 3. 주요 기능

### 3-1. 샘플 시스템
- **샘플**: 포켓몬 1마리의 완성된 육성 구성 (특성·성격·도구·기술·노력치·테라타입·메모·파트너·카운터)
- **샘플 이름 형식**: `[별명(선택)] {포켓몬이름} 샘플` — 접미사 고정·수정 불가, 별명만 입력 가능
  - 영어 모드 표시: `[nickname] {EnglishName} Set`
  - DB 저장은 항상 한국어 형식, 영어 표시는 런타임 변환 (`buildSampleDisplayName`)
- 비로그인 익명 등록 가능 (익명 표시명 자동 생성)
- 좋아요 기반 인기도 산출
- **댓글 기능**: 로그인 유저만 댓글 작성 가능 (`StandaloneSampleComment` 모델)
- 포맷(싱글/더블) 구분
- 파트너/카운터 포켓몬 태그 — 메가진화폼 포함, 화살표 키 네비게이션, Enter 선택

### 3-2. 팀 빌딩
- 최대 6마리 팀 구성
- 팀 내 각 포켓몬에 샘플 연결
- 렌탈 코드 10자리 기록 가능
- **팀 작성 폼에서 메가스톤 선택 시 해당 메가폼으로 자동 전환** (SampleCard 양방향 연동)
- **팀 상세 페이지에서 base 폼 + 메가스톤 조합 시 메가폼 이름·스프라이트로 올바르게 표시**

### 3-3. OCR 자동 등록 (현재 비활성화 — 2026-05-11)
- **상태:** Railway 무료 트라이얼 만료로 OCR 서버 운영 중단. 샘플 등록 폼의 "OCR로 불러오기" 버튼·다이얼로그 숨김 처리. `/api/ocr` 라우트는 503 응답.
- **재활성화 방법:** `ocr_server/Dockerfile` 그대로 다른 PaaS(Fly.io, Cloud Run 등)에 배포 → Vercel `OCR_SERVER_URL` 환경변수 새 URL로 갱신 → `SampleNewForm.tsx`에서 `SampleOCR` 임포트·버튼 복원 → `/api/ocr/route.ts`를 이전 프록시 코드로 되돌리기.
- (기존 사양) 스위치 캡쳐사진 업로드 → EasyOCR 서버가 포켓몬 이름·기술·노력치 자동 인식, learnset 기반 필터링, 빈값 fallback, 이미지 회전, 노력치 합계 표시.

### 3-4. 대미지 계산기
- Gen 9 규칙 적용
- 공격자 / 수비자 포켓몬 선택, SP 배분, 성격, 랭크 보정, 도구
- 날씨 보정: 쾌청·비·모래바람·설경
- 필드 보정: 그래스·미스트·사이코·일렉트릭 (타입 강화 + 미스트의 드래곤타입기술 약화, 그래스의 지진·땅고르기 약화)
- 벽 보정: 리플렉터·빛의장막·오로라베일 (급소 시 무효)
- 솔라빔·솔라블레이드: 쾌청 이외 날씨 위력 절반
- 무조건 급소 기술 자동 체크
- 특성 보정: 약 60종 이상 반영 (특성 이름은 DB 포켓몬 데이터에서 가져옴)
- 멀티스케일: 수비자 특성으로 멀티스케일 선택 시 체크박스 표시 → 활성화하면 받는 데미지 ×0.5 계산
- **보정값 칩 표시**: 계산 결과에 적용된 모든 보정(자속보정·타입상성·날씨·필드·벽·급소·멀티스케일 등)을 컬러 칩(pill)으로 표시 — 강화(배율 >1) emerald, 약화(배율 <1) orange
- 탁쳐서떨구기: 수비자가 도구를 지니고 있으면 위력 ×1.5 (수비자 도구 선택 시 자동 적용)
- 바디프레스: 공격자 방어 수치로 계산
- 지닌도구 지원, 공격자/수비자 스왑 시 도구 교환
- Combobox 화살표 키 네비게이션
- 샘플 불러오기로 등록된 샘플의 스탯 자동 적용
- 실수치 공식: HP = 종족치+75+SP, 나머지 = floor((종족치+20+SP) × 성격보정)
- **수비자 UI 아이템/특성 순서**: 아이템 먼저, 특성 나중 표시
- **일본어 모드에서 특성명**: `/api/data/abilities` API에서 ja 필드를 가져와 일본어로 표시
- 기술·도구·포켓몬 명칭은 모두 DB 데이터 기준으로만 사용 (하드코딩 금지)
- **아이템 위력 보정**: 18종 타입 부스트 도구(목탄·신비의물방울·자석·기적의씨·녹지않는얼음·검은띠·독바늘·부드러운모래·예리한부리·휘어진스푼·은빛가루·딱딱한돌·저주의부적·용의이빨·검은안경·금속코트·요정의깃털·실크스카프) 동일 타입 기술 ×1.2 반영, 계산 결과에 도구 라벨 독립 표시
- **전기구슬**: 피카츄 소지 시 공격/특공 ×2 자동 반영
- **결정력(ketteiryoku) 집계**: 공격(특공) 실수치 × 기술위력 × 특성보정 × 공격자측 버프 전부. 버프 요소 — 날씨(쾌청+불꽃/비+물 ×1.5), 필드(일렉트릭/사이코/그래스 ×1.3), 충전 ×2.0, 도우미 ×1.5, 급소 ×1.5, 타입 부스트 도구 ×1.2, 탁쳐서떨구기 ×1.5. 자속(STAB)은 별도 수치로 표기(적응력 특성 시 ×2.0). 반감 요소는 결정력 계산 제외
- **공격자만 결정력**: 수비자 미선택 상태에서도 공격자+기술+조건만으로 결정력 숫자 즉시 표시 (탁쳐서떨구기 제외, 그 외 버프는 모두 반영)

### 3-5. 사용률 통계
- 샘플 등록/수정/삭제 시 자동 집계 (`UsageStat` 테이블)
- countBased(샘플 수) / weightedBased(좋아요 수) 두 가지 기준
- 포켓몬별 인기 기술·도구·특성·노력치 Top 3 표시
- 코어 조합(자주 함께 쓰는 포켓몬) 분석

### 3-6. 패치노트 시스템
- **PatchNote 모델**: `title`, `content`, `category(String?)`, `createdAt`
- **카테고리**: `"feature"` (기능추가 · 초록) / `"bug"` (버그수정 · 빨강) / `null` (미분류 · 기본색)
- **메인 패널** (`PatchNotePanel`): 최신 5개 표시, 하단 "전체 패치노트 보기 →" 링크
- **전체 패치노트 페이지** (`/patch-notes`): 아코디언 형식, 제목(좌) + 날짜·화살표(우), 1줄 설명은 Y축 중앙정렬, 10개씩 페이지네이션
- **캐시**: `Cache-Control: no-store` — 관리자 수정 즉시 반영 필수
- **관리자 UI** (`PatchNoteManager`): 카테고리 토글 버튼(기능추가/버그수정/선택안함)으로 등록·수정 가능

### 3-7. 버그 신고 시스템
- 신고 카테고리: 포켓몬 정보 오류·기술 정보 오류·도구 오류·계산기 오류·페이지 오류·비속어 신고
- 비로그인·로그인 모두 신고 가능, 한국어 비속어 필터 적용
- 관리자 처리 상태: open / 처리중 / 완료 / 보류

---

## 4. 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js 16 (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS (zinc 계열 다크모드) |
| DB | Neon PostgreSQL (pokenova와 공유) |
| ORM | Prisma |
| 인증 | NextAuth.js (Google OAuth) |
| 배포 | Vercel |
| OCR 서버 | Python FastAPI + EasyOCR (Railway, **2026-05-11 비활성화**) |

---

## 5. 데이터 모델 요약

```
User ──────────────────────────────────────────────
 ├── Team (팀) ──▶ TeamSample[] ──▶ StandaloneSample
 ├── StandaloneSample (독립 샘플)
 ├── TeamLike / StandaloneSampleLike
 └── 닉네임, 관리자 여부

PokemonData ──────── move, item, ability, learnset
UsageStat ─────────── 포켓몬별 집계 데이터
PatchNote ─────────── 관리자 패치노트
BugReport ─────────── 버그/오류 신고 (category, status, title, content)
```

---

## 6. 페이지 구조

| 경로 | 설명 |
|---|---|
| `/` | 메인 (샘플 Top, 최신 팀) |
| `/samples` | 샘플 목록 (검색·필터·정렬) |
| `/samples/new` | 샘플 등록 (OCR은 비활성화, 수동 입력만) |
| `/samples/[id]` | 샘플 상세 |
| `/samples/[id]/edit` | 샘플 수정 |
| `/teams` | 팀 목록 |
| `/teams/new` | 팀 등록 |
| `/teams/[id]` | 팀 상세 |
| `/calc` | 대미지 계산기 |
| `/patch-notes` | 전체 패치노트 (아코디언·페이지네이션) |
| `/stats` | 포켓몬 사용률 통계 |
| `/stats/[id]` | 포켓몬 통계 상세 |
| `/my` | 마이페이지 |
| `/report` | 버그/오류 신고 |
| `/admin` | 관리자 (패치노트 관리, 전체 유저 목록, 통계 집계, 신고 처리) |
| `/setup-nickname` | 닉네임 설정 (최초 로그인 시) |
| `/sign-in` | 로그인 |
| `/users/[id]` | 유저 프로필 |

---

## 7. UI 디자인 원칙

- **배경:** `#09090b` (zinc-950), 카드: zinc-900, 테두리: zinc-800
- **포켓몬 타입별 카드 테두리 색상** — 첫 번째 타입 색 적용
- **계산기:** 공격 패널 빨강(`border-red-500/40`), 수비 패널 파랑(`border-blue-500/40`)
- **노력치 슬라이더:** 공격 계열 빨강, 수비 계열 파랑 accent
- **성격 그리드:** 5×5 table-fixed, 성실(중립)은 대각선에 별도 표시
- **모바일:** 햄버거 메뉴 (오른쪽 슬라이드), input 포커스 시 자동 확대 방지
- **브랜딩:**
  - 파비콘: `MEGA_SIMBOL.webp` (`public/MEGA_SIMBOL.webp`)
  - 네비게이션 바: 타이틀 왼쪽에 MEGA_SIMBOL 로고 이미지 (데스크탑 28px, 모바일 24px)
- **아바타:** Google OAuth 프로필 이미지 — 네비바·등록자 표시·관리자 유저 목록에 사용
- **메인 페이지 샘플 섹션**: 섹션 헤더 우측에 "당신의 샘플을 공유하세요" 문구 + "+ 샘플 추가" indigo 버튼 → `/samples/new`
- **OG / SNS 공유 메타태그 (영문):**
  - title: `"Pochams — Pokémon Champions Sample & Calculator"`
  - description: `"Pochams — Pokémon Champions Sample & Calculator | 포켓몬 챔피언스 샘플 공유 & 계산기"`
  - OG 이미지: "Pochams" 대형 텍스트 + "Pokémon Champions Sample & Calculator" 부제
  - X(트위터) 등 SNS 공유 시 영문 우선 표시

---

## 8. SEO / 분석

- **Google Analytics 4:** G-B03JQ26Q27 연결
- **Google Search Console:** 소유권 인증 완료, sitemap.xml 제출
- **네이버 서치어드바이저:** 소유권 인증 완료, sitemap.xml 제출
- **OG 이미지:** "Pochams" 영문 텍스트 기반 1200×630 이미지 (공유 미리보기용, next/og ImageResponse)
- **sitemap.xml:** `/`, `/teams`, `/samples`, `/calc`, `/stats` 포함
- **Canonical URL:** root layout에 `metadataBase: https://pochams.com`만 박고 canonical은 페이지별로 명시 (Next.js metadata는 shallow merge라 root에 canonical을 박으면 자식 미명시 페이지가 전부 상속받아 홈 중복으로 판정됨). 모든 공개 페이지(`/`, `/teams`, `/samples`, `/calc`, `/stats`, `/pokedex`, `/patch-notes`)에 `alternates.canonical: '/<path>'` 명시. 동적 상세 페이지(`/samples/[id]`, `/teams/[id]`, `/stats/[id]`)는 generateMetadata로 self-canonical 동적 생성. patch-notes는 client component라 부모 layout.tsx 신설해서 처리
- **색인 차단:** `robots.ts` disallow + 각 server component 페이지 `metadata.robots: { index: false, follow: false }` 이중 방어. 차단 대상은 `/admin`, `/api/`, `/sign-in`, `/my`, `/setup-nickname`, `/report`, `/users/`, `/samples/new`, `/samples/*/edit`, `/teams/new`, `/teams/*/edit`

---

## 9. 개발 히스토리 요약 (주요 마일스톤)

| 날짜 | 내용 |
|---|---|
| 2026-04-09 | 프로젝트 초기 세팅, DB 스키마, 포켓몬 데이터 연동 |
| 2026-04-10 | 팀/샘플 CRUD, 마이페이지, 통계, 메가진화 계산기 |
| 2026-04-11 | OCR 샘플 등록, 계산기 UX 개선, 비로그인 등록 허용 |
| 2026-04-12 | 통계 집계 API, OCR 개선, zinc 다크모드 전환 |
| 2026-04-13 (전반) | 계산기 날씨·벽·급소 규칙, Combobox 키보드 내비게이션, 성격 UI |
| 2026-04-13 (후반) | 버그 신고 시스템, GA4·SEO·OG 이미지, 모바일 최적화, OCR 다수 개선 |
| 2026-04-14 (전반) | 샘플/팀 싱글·더블 포맷 필터 추가, API 코드 최적화, 복수 버그 수정 |
| 2026-04-14 (후반) | 계산기 실수치 공식 버그 수정, 특성·필드 효과 버그 수정, DB 명칭 규칙 정립 |
| 2026-04-15 (전반) | Vercel 리전 sin1(싱가포르), CDN 캐시, 기술·도구 한/영 표시, 댓글·아바타, OCR 드롭다운 개선, 샘플 상세 레이아웃 |
| 2026-04-15 (후반) | 샘플 이름 suffix 고정 UX + 영어 모드 `{영어이름} Set` 전체 적용 + 기존 DB 샘플 이름 정규화 + 파비콘·네비바 로고(MEGA_SIMBOL) + 한/영 언어 전환 main 머지·배포 완료 + OG 메타태그 한영 병기 |
| 2026-04-16 (전반) | 영어 버전 통계 한국어 잔재 수정 — SpTop3 엣지케이스 + StatsRow 기술/도구/특성/샘플 이름 GameNamesContext 번역 적용 |
| 2026-04-16 (후반-1) | 대미지 계산기 멀티스케일 적용 버튼 추가 — 수비자 특성 선택 시 체크박스 자동 노출, 활성화 시 받는 데미지 ×0.5 |
| 2026-04-16 (후반-2) | 계산 결과 보정값 칩 표시 (자속/타입상성/날씨/필드/벽/급소/멀티스케일 등 emerald·orange 컬러 칩) |
| 2026-04-16 (후반-3) | 패치노트 전용 페이지(/patch-notes) 신설 — 아코디언·페이지네이션, 헤더 nav 추가, 메인 패널 5개 제한 + 전체보기 링크 |
| 2026-04-16 (후반-4) | 패치노트 카테고리 기능 — PatchNote.category(feature/bug) 스키마 추가, 관리자 UI 토글 버튼, 패치노트 제목 색상 구분(초록/빨강), CDN 캐시 no-store 수정 |
| 2026-04-16 (후반-5) | 관리자 페이지 전체 유저 목록 추가 — 프로필 사진·검색·페이지네이션, 최근 가입 유저에도 아바타 표시 |
| 2026-04-16 (후반-6) | 메인 페이지 단일 샘플 섹션 헤더에 "당신의 샘플을 공유하세요" 문구 + "+ 샘플 추가" 버튼 추가 |
| 2026-04-16 (후반-7) | OG 이미지·메타데이터 영문화 — title/description/og-image 모두 영문("Pochams — Pokémon Champions Sample & Calculator")으로 변경 |
| 2026-04-16 (후반-8) | 코드베이스 오류 수정 및 최적화 — isAdmin 공통 유틸(lib/isAdmin.ts) 중앙화, 팀 수정 API $transaction 원자화, fetch 에러 핸들링 추가 |
| 2026-04-16 (후반-9) | 팀 댓글 기능 완성 — TeamCard 댓글 수 표시, getTeamList에 _count.comments 포함 |
| 2026-04-16 (후반-10) | 도감 메가플라엣테 누락 버그 수정 — CHAMPIONS_MEGA_ONLY_ENS 도입, getPokedexEntries 메가 쿼리 inChampions 직접 필터로 개선 |
| 2026-04-16 (후반-11) | 팀 댓글 익명 팀 차단 버그 수정 — team.authorId 조건 제거, 모든 팀에 댓글 허용 |
| 2026-04-18 | 비로그인 팀 작성자 anonName 표시, 관리자 닉네임 미설정 뱃지, 메인 샘플 추가 버튼 항상 표시, 포켓몬 API formLabel/regionType 자동 주입 |
| 2026-04-19 (전반) | 계산기 탁쳐서떨구기 도구 소지 시 위력 ×1.5 적용, 도메인 pochams.com 연결 완료 처리, KO/JA/EN 3개 언어 지원 완성 — 계산기·도감·샘플 전체 일본어화, Prisma ja 필드 + PokeAPI 연동 |
| 2026-04-19 (후반) | 일본어 2차 현지화 완성 — T컴포넌트 ja prop 추가, 메인/팀목록·상세/샘플목록/통계목록·상세/계산기/오류신고/패치노트 전 페이지 일본어화, BATTLE_FORMAT_LABEL_JA 추가, 패치노트 일본어 선택 시 영어 버전 표시 |
| 2026-04-19 (3차) | 일본어 3차 현지화 완성 — SampleNewForm/SampleCard/TeamCard/PokemonSlotForm/MySamplesModal/setup-nickname/팀 edit 클라이언트 전부 일본어화, suffix 'サンプル' 버그 수정, 전체 UI 일본어 지원 완료 |
| 2026-04-20 | 계산기 개선 — 수비자 메가진화 시 메가스톤·특성 자동 적용, 아이템/특성 순서 변경, alt폼 스탯 오류 수정(id < 100000 필터), 계산기 일본어 특성명 API 연동, upsert 스크립트 추가(npm run upsert) |
| 2026-04-21 | 세레비넷 비교 후 누락 기술 683개 추가(185마리 전수), learnsets API 캐시 3600→300초 단축, 세레비넷 learnset 비교 가이드 문서화, 팀 상세/작성 폼 메가폼 자동 표시·전환 수정, dead code 3개 파일 제거 |
| 2026-04-24 | 계산기 결정력 표시값 특성 보정 미반영 버그 수정(effAtk×power×atkMod.multiplier로 변경) — 철주먹/예리함/메가런처 등 위력 배율 특성이 결정력에 반영되도록 수정, 페어리스킨/프리즈스킨/스카이스킨 3종 특성 추가(노말 기술 ×1.2) |
| 2026-04-25 | 메가플라엣테 누락 복구 — DB inChampions stale 문제로 계산기에서 미노출, `npm run upsert pokemon floette`로 base/메가/영원의꽃 재반영. 추가로 getTagPokemonOptions 비대칭 필터 버그 수정(메가 row의 baseIds 의존 제거, 알트폼과 동일하게 inChampions 직접 필터로 통일) — 샘플 파트너/카운터 태그에서도 메가플라엣테 노출. /api/data/pokemon 라우트에도 동일한 비대칭 버그 수정(계산기 ?championsOnly=true&includeMega=true 응답에서 메가플라엣테 누락 근본 원인). 도감 메가 카드 클릭 시 popupId를 entry.id 그대로 사용해 메가별 통계 팝업 표시(기존엔 강제 base id 변환으로 메가눈여아 → 눈여아 통계가 뜸) |
| 2026-05-08 | SEO — Search Console "사용자가 선택한 표준이 없는 중복 페이지" 경고 대응. (1차) root layout에 metadataBase + alternates.canonical 박고 sitemap 등록 4개 페이지(/teams /samples /calc /stats)에 page-specific canonical 명시. /admin /my /users /samples/new /teams/new /teams/*/edit에 metadata.robots noindex + robots.ts disallow 이중 차단. client component 페이지(/sign-in /setup-nickname /report /samples/[id]/edit)는 robots.ts disallow로만 처리. (2차) Next.js metadata shallow merge로 root canonical이 자식에 상속되는 cascade 버그 발견 — root에서 canonical 제거하고 모든 공개 페이지(홈 / pokedex / patch-notes / samples/[id] / teams/[id] / stats/[id])에 명시적 canonical 추가. patch-notes는 client component라 layout.tsx 신설 |
| 2026-05-11 | OCR 기능 비활성화 — Railway 무료 트라이얼 만료(2026-05-12)로 EasyOCR 서버 운영 중단. `SampleNewForm.tsx`에서 OCR 버튼·다이얼로그·`SampleOCR` 임포트·`handleOCRFill`·`showOCR` 상태 제거, `/api/ocr/route.ts`를 503 응답으로 단축. `ocr_server/` 폴더와 `OCR_SERVER_URL` env 변수는 그대로 둬서 다른 PaaS로 옮기면 즉시 복구 가능 |
| 2026-06-24 | 계산기 변환자재/리베로 자속 토글 추가 — 변환자재(protean)·리베로(libero) 보유 포켓몬 선택 시 '특성·도구 효과' 칸에 on/off 토글 노출. ON이면 모든 기술을 자속으로 간주해 데미지 ×1.5 적용(`isStab`을 강제 true). 원래 자속 기술은 이미 isStab=true라 중복 없이 1.5배 한 번만 적용. 결정력·실데미지 두 블록 동시 반영, `DamageCalc.tsx` 단일 파일 수정, lint 0·verify_calc·build 통과 |
| 2026-06-23 | 계산기 반감열매 버그 수정 — 오카열매류 type-protection 18종 중 초나열매(wacan)·린드열매(rindo)의 반감 타입이 서로 뒤바뀐 오류 교정. `data/quiz_items.json` desc(초나=전기/린드=풀) + `DamageCalc.tsx` BERRY_RESIST 동일 수정, verify_calc·build·lint 0 통과. 검증 중 `upsert.ts` 독립 누락 버그 발견 — DATA_DIR이 `pokenova_project/data`, SPRITE_BASE가 `pokenova.vercel.app`을 가리켜 seed.ts와 다른 소스를 읽고 있었음. `../data`·`""`로 교정해 단건 upsert가 옛 데이터·pokenova 절대 sprite URL을 덮어쓰던 문제 해소, DB 전수 오염 0 검증 |
| 2026-06-20 | 데이터 동기화 브릿지 구축 — 시즌 데이터 추가를 "JSON 한 곳 수정 + `npm run sync` 한 명령"으로 포챔스(DB seed)·포케노바(static push) 동시 반영. 챔피언스 포켓몬 목록을 `champions_pokemon_ids.json` 단일 원천으로 통합(seed.ts·재생성 스크립트 공유), seed.ts 하드코딩 ID 3종 제거. 인라인 파이썬을 `scripts/regen_champions_learnsets.py`로 분리하며 seed.ts vs CLAUDE.md 간 670 불일치 정리(altOnly/megaOnly 원종 ID 자동 유도로 양쪽 동작 보존). 재생성 결과 기존과 완전 동일 검증(186마리·11542기술) |
| 2026-06-20 | 시즌3(레귤레이션 M-B) 데이터 반영 — 포켓몬 20종(일반 19 + 찌르호크) 챔피언스 추가, 기술 5종·도구 31종(도구 15 + 메가스톤 16) in_champions ON. 방침: md를 정답지로 포켓몬별 `learnsets.json` 기술폭을 통째 교체(프론트가 learnset의 en만 사용 확인). md에 없던 찌르호크 기술폭은 pokemon.fandom.com API로 보강. 원본 md는 `docs/champions_seasons/`에 시즌별 보관(README 추적 인덱스). 챔피언스 포켓몬 186→206마리. ※ 기술 위력/PP 스탯 동기화는 계산기 영향으로 후속 과제 |
| 2026-06-20 | **포챔스 전용 DB 구축** — 포케노바 공유데이터와 분리한 자체 데이터(`pochams_project/data/`) 신설. 포켓몬위키 `Champions 기술` 문서를 챔피언스 205종 전부 수집해 `pochams_pokemon.json`(이름·종족값·타입·특성·기술폭) / `pochams_moves.json`(분류·카테고리배열·위력·명중·우선도·접촉·효과) / `pochams_abilities.json`(이름·효과) 생성. 도구는 기존 챔피언스 도구 재사용. 위키(기술폭·챔피언스 스탯)+기존데이터(접촉/효과/종족값/특성효과) 하이브리드, 미매칭 0. `scripts/build_pochams_db.py`. ※ Neon 적재·사이트 연동은 다음 단계 |
| 2026-06-20 | **대규모 개편 — 레퍼런스 도구로 전환.** 샘플 등록·팀·상세 사용률(/stats)·마이페이지·유저·공개 로그인/회원가입 전부 제거. 도감·대미지 계산기·주간 사용률 Top10(홈, admin 수동 입력)·버그신고·패치노트만 유지. 관리자 인증은 Google+ADMIN_EMAIL 유지(공개 로그인 없음, /sign-in은 관리자 전용). DB에서 회원·샘플·팀·UsageStat 삭제(백업 보관), 마스터·주간랭킹·패치노트·버그신고 유지. 계산기 '내 샘플 불러오기/인기기술' 제거, 도감 통계 팝업 제거. Prisma 미사용 모델은 마이그레이션 리스크로 데이터만 비우고 정의는 유지 |
| 2026-06-21 | **리팩터 잔재 미사용 코드 일괄 정리(−2132줄).** Explore 에이전트로 참조 0건 확인 후 3개 등급 제거. (A) 죽은 코드 — `queries.ts`(팀·샘플·통계 함수 10개)·`aggregateStats.ts` 파일 전체, 미사용 shared `NatureSelector.tsx`, `buildSampleDisplayName` + stale import. (B) 공개 화면 없는 기능 전부 제거 — **주간랭킹·패치노트·GameNamesContext**(모델·API·관리자 매니저·layout provider·admin 섹션). admin은 버그신고만 유지. (C) 비코드 잔재 — OCR 인프라(`ocr/`·`ocr_server/`·`public/ocr/`·`kor.traineddata`) + `tesseract.js` 의존성, 중복 `.env.vercel.*` 5개, 일회성 마이그레이션 스크립트 4개(backups/ 보존). Prisma 모델 11개 + `BattleFormat` enum 제거 후 `db push`(patch_notes·weekly_rankings 테이블 drop). 빌드 통과. **이어서 비계산기 lint 에러 정리** — seed.ts/upsert.ts의 `any` 27개를 소스 JSON 인터페이스 + `readJson` 제네릭 + `Prisma.PokemonDataCreateManyInput`로 제거, gameData 미사용 `baseIds` 제거(에러 51→24). 남은 24개는 계산기(DamageCalc) React 훅 규칙으로, **계산기를 UI/UX부터 새로 만들 때** 처리 예정(데미지 계산 결과·공식은 현재 값 보존, verify_calc.mjs 통과 유지). ⭐ 다음 작업 = 계산기 재작성 |
| 2026-06-21 | **계산기 재디자인 + 데미지 영향 도구·특성 확장.** (1) 재디자인 — 계산 엔진 그대로 두고 화면만 배틀 HUD 다크 테마로(Oxanium 폰트·공격 로즈/수비 스카이 카드·대형 데미지 readout+HP게이지·KO 배지·흰 배경 버그 수정), CheckBox 분리로 static-components 11개 해소, 헤더 아이콘 제거·'기술&조건'→'기술'. (2) 도구 추가 — 힘의머리띠 물리×1.1·박식안경 특수×1.1·달인의띠 효과굉장×1.2·타입반감열매 18종(수비자 ×0.5)·메트로놈 0~5(×1.0~2.0). (3) 특성 추가 — 우격다짐×1.3·의욕 물리×1.5·선파워 쾌청 특공×1.5·근성 토글×1.5·총대장 0/1/2. '특성·도구 효과' 활성화 칸 신설. (4) 스피드 실수치 반영 — 구애스카프×1.5·날씨/필드 특성×2·트리거형 토글. 결정력·실데미지 두 블록 일관 반영, verify_calc·build 통과. (5) 수비 특성 3종 추가(퍼코트·정화의소금·건조피부) + 계산기 React 훅 lint 13개 정리(set-state-in-effect 의도적 disable, useMemo 누락 의존성 보강으로 staleness 버그까지 해소) → 세션 시작 51개에서 **lint 0 problems** 달성 |
| 2026-06-21 | **도구·특성 도감 신설 + 영/일 임시 비활성.** `/items` 도구 도감(이미지+이름+효과, 검색·카테고리 필터, 챔피언스 148) · `/abilities` 특성 도감(이름+설명+보유 포켓몬, 검색·초성 ㄱㄴㄷ 필터, 챔피언스 194). 네비게이션 = 도감·**도구·특성**·계산기·신고. 도구 이미지 148개를 포켓몬위키에서 재수집·교체(`scripts/fetch_item_images.py`, 아이콘 9세대/ZA 파일 직접 탐색, WebP→PNG 변환). 영어·일본어는 번역 미완으로 `LANG_LOCKED`(LangContext) 플래그로 임시 비활성(한국어 고정, 토글 숨김, false로 복구). build·lint 0 통과 |

---

## 10. 다국어(i18n) 언어 전환 기능

> **목적:** 해외 유저 접근성 향상 — 브라우저 언어 자동 감지, KO/JA/EN 3개 토글 버튼 제공  
> **상태:** ✅ KO/EN 완료(main) + JA 전 페이지 완료(feature/japanese 브랜치 — main 머지 예정)  
> **도메인:** https://pochams.com

### 10-1. 구현 방식

**브랜치:** feature/japanese (일본어 개발 중 — main 미머지)

**언어 결정 우선순위:**
1. URL 파라미터 `?lang=en` / `?lang=ko` / `?lang=ja` (공유 링크 최우선)
2. localStorage 저장값 (재방문 시 이전 선택 유지)
3. `navigator.language` 브라우저 언어 감지 (ko → 한국어, ja → 일본어, 나머지 → 영어)

**핵심 파일:**
| 파일 | 역할 |
|---|---|
| `src/contexts/LangContext.tsx` | React Context — `lang`, `setLang`, 감지 로직 |
| `src/lib/i18n.ts` | UI 텍스트 번역 사전 (nav, 버튼, 섹션 제목 등) |
| `src/components/shared/T.tsx` | `<T ko="한국어" en="English" />` 인라인 번역 컴포넌트 |
| `src/components/shared/Header.tsx` | KO / JA / EN 3개 토글 버튼 |
| `src/app/layout.tsx` | LangProvider 래핑, `<html lang>` 동적 반영 |

### 10-2. 데이터 필드 전환

DB에 `ko`/`en`/`ja` 필드가 존재한다. 언어에 따라 전환:

```tsx
const { lang } = useLang()
pokemon[lang]  // 'ko' → "리자몽", 'en' → "Charizard", 'ja' → "リザードン"
```

**샘플 이름 다국어 변환 유틸:** `src/lib/nameMaps.ts` — `buildSampleDisplayName(sampleName, pokemonKo, pokemonEn, lang, pokemonJa?)` 함수로 DB 저장된 한국어 이름을 런타임에 영어/일본어 형식으로 변환.  
- 영어: `"{영어이름} Set"` 형식  
- 일본어: `"{일본어이름} サンプル"` 형식  
- 한국어: DB 저장값 그대로

**일본어 명칭 소스:**
| 분류 | 출처 |
|---|---|
| 포켓몬 이름 | PokeAPI `ja-Hrkt` 필드 (katakana) |
| 기술·도구·특성 | PokeAPI `ja-Hrkt` 필드 |
| 성격 | `nameMaps.ts` NATURE_JA 수동 정의 |
| 타입 | `nameMaps.ts` TYPE_JA 수동 정의 |

### 10-3. 작업 체크리스트

- [x] `LangContext.tsx` — KO/JA/EN 3개 언어 감지·저장
- [x] `i18n.ts` — KO/JA/EN 전체 UI 텍스트 번역 사전
- [x] `Header.tsx` — KO / JA / EN 3개 토글 버튼 (데스크탑·모바일)
- [x] Prisma 스키마에 `ja String @default("")` 추가 (PokemonData, MoveData, ItemData, AbilityData)
- [x] PokeAPI 스크립트로 일본어 명칭 수집·DB 반영
- [x] `nameMaps.ts` — NATURE_JA, TYPE_JA export
- [x] `GameNamesContext.tsx` — translateMove/Item/Ability/Nature/Type 일본어 지원
- [x] `DamageCalc.tsx` — 계산기 전체 일본어화 완료
- [x] `PokedexClient.tsx` / `PokedexPopup.tsx` — 도감 일본어화 완료
- [x] 샘플 이름 일본어 suffix (`buildSampleDisplayName` 확장)
- [x] `PokedexEntry` 타입에 `ja` 필드 추가
- [x] `feature/japanese` → main 머지 완료 (2026-04-19)
- [x] 커스텀 도메인 pochams.com 연결 완료
