# 포챔스 (Pochams Battle) 기획서

> **버전:** 9.21  
> **최종 업데이트:** 2026-09-10  
> **⚠️ 2026-06-20 대규모 개편:** 커뮤니티(샘플·팀 공유)·로그인·상세통계를 제거하고 **도감 · 대미지 계산기 · 주간 사용률 Top10 · 버그신고 · 패치노트** 중심의 레퍼런스 도구로 전환. 아래 본문의 샘플/팀/통계/i18n·유저 관련 섹션은 개편 이전 기준이며 추후 정리 예정.  
> **배포 URL:** https://pochams.com  
> **구 URL:** https://pochams-battle.vercel.app → pochams.com으로 301 리다이렉트  
> **레포지토리:** github.com/soondoree07/pochams_project

---

## 1. 프로젝트 개요

포챔스 배틀은 **포켓몬 챔피언스** 게임 플레이어를 위한 종합 전략 플랫폼이다.  
샘플(세부 육성 구성) 공유, 팀 빌딩, 대미지 계산, 사용률 통계 기능을 하나의 사이트에서 제공한다.

### 핵심 가치
- 비로그인으로도 샘플 등록·팀 빌딩 가능 (익명 참여 허용)
- 커뮤니티 좋아요 기반 인기 샘플 큐레이션
- 정확한 대미지 계산기 (Gen 9 규칙 적용)

---

## 2. Pokenova와의 관계

### 2-1. 데이터 공유 구조

```
pokenova_project/data/          pochams_project (DB)
─────────────────────────       ──────────────────────────
quiz_pokemon.json       ──seed──▶  pokemon_data 테이블
quiz_moves.json         ──seed──▶  move_data 테이블
quiz_items.json         ──seed──▶  item_data 테이블
quiz_abilities.json     ──seed──▶  ability_data 테이블
learnsets.json          ──seed──▶  pokemon_learnset 테이블
```

- **pokenova가 원천 데이터 관리자.** 포켓몬·기술·도구·특성·배우는기술 데이터는 pokenova 레포의 JSON 파일에서 관리한다.
- **pochams는 그 데이터를 DB에 seed해서 사용.** `npm run seed` 한 번으로 모든 마스터 데이터가 갱신된다.
- 두 프로젝트는 **Neon PostgreSQL 하나를 공유**한다. DB 수정은 pokenova JSON → seed 재실행 순서로만 한다.
- **동기화 브릿지 `npm run sync` (2026-06-20, 2026-07-09 확장):** 포케노바 `pokedex.json`의 `inChampions` 플래그 수정 후 한 명령으로 ⓪브릿지(pokedex.json→champions_pokemon_ids.json) ①위키 Champions 기술폭 수집 ②포챔스 소스 생성 ③champions_learnsets 재생성 ④포챔스 DB seed ⑤포케노바 data 커밋까지 수행. `--push`면 포케노바 배포(git push)까지. 포챔스는 DB에서 읽어 seed 즉시 반영(재배포 불필요), 포케노바는 정적 사이트라 push 후 재배포.

### 2-2. 역할 분리

| 항목 | pokenova | pochams |
|---|---|---|
| 역할 | 포켓몬 도감 + 퀴즈 플랫폼 | 팀 빌딩 + 샘플 공유 플랫폼 |
| 마스터 데이터 관리 | ✅ JSON 파일로 관리 | ❌ (읽기만) |
| 유저 계정 | ❌ | ✅ NextAuth |
| 커뮤니티 기능 | ❌ | ✅ 좋아요, 팀, 샘플 |
| 계산기 | ❌ | ✅ 대미지 계산기 |
| 배포 플랫폼 | Vercel (pokenova.pochams.com) | Vercel (pochams.com) |

### 2-3. 챔피언스 전용 필터

pokenova의 기술·도구 데이터에는 `in_champions` 플래그가 있다.  
포켓몬의 챔피언스 노출 여부는 **포케노바 `data/pokedex.json`의 `inChampions` 플래그가 단일 원천**이다(2026-07-09~). 브릿지 `scripts/champions_from_pokedex.py`가 이걸 읽어 `champions_pokemon_ids.json`(baseIds + altOnlyEns/megaOnlyEns)을 생성하고, `seed.ts`·`regen_champions_learnsets.py`는 그 생성물을 읽는다. 포켓몬 추가/제거 = `pokedex.json`에서 `inChampions` on/off 후 `npm run sync`.  
pochams는 이 플래그/목록이 `true`·포함인 데이터만 사용해 포켓몬 챔피언스 범위로 제한한다.  
단, 관련 포켓몬(파트너/카운터) 태그 입력에는 메가진화폼도 포함된다(`includeMega=true`).

---

## 3. 주요 기능

### 3-1. 샘플 시스템
- **샘플**: 포켓몬 1마리의 완성된 육성 구성 (특성·성격·도구·기술·노력치·테라타입·메모·파트너·카운터)
- **샘플 이름 형식**: `[별명(선택)] {포켓몬이름} 샘플` — 접미사 고정·수정 불가, 별명만 입력 가능
  - 영어 모드 표시: `[nickname] {EnglishName} Set`
  - DB 저장은 항상 한국어 형식, 영어 표시는 런타임 변환 (`buildSampleDisplayName`)
- 비로그인 익명 등록 가능 (익명 표시명 자동 생성)
- 좋아요 기반 인기도 산출
- **댓글 기능**: 로그인 유저만 댓글 작성 가능 (`StandaloneSampleComment` 모델)
- 포맷(싱글/더블) 구분
- 파트너/카운터 포켓몬 태그 — 메가진화폼 포함, 화살표 키 네비게이션, Enter 선택

### 3-2. 팀 빌딩
- 최대 6마리 팀 구성
- 팀 내 각 포켓몬에 샘플 연결
- 렌탈 코드 10자리 기록 가능
- **팀 작성 폼에서 메가스톤 선택 시 해당 메가폼으로 자동 전환** (SampleCard 양방향 연동)
- **팀 상세 페이지에서 base 폼 + 메가스톤 조합 시 메가폼 이름·스프라이트로 올바르게 표시**

### 3-3. OCR 자동 등록 (현재 비활성화 — 2026-05-11)
- **상태:** Railway 무료 트라이얼 만료로 OCR 서버 운영 중단. 샘플 등록 폼의 "OCR로 불러오기" 버튼·다이얼로그 숨김 처리. `/api/ocr` 라우트는 503 응답.
- **재활성화 방법:** `ocr_server/Dockerfile` 그대로 다른 PaaS(Fly.io, Cloud Run 등)에 배포 → Vercel `OCR_SERVER_URL` 환경변수 새 URL로 갱신 → `SampleNewForm.tsx`에서 `SampleOCR` 임포트·버튼 복원 → `/api/ocr/route.ts`를 이전 프록시 코드로 되돌리기.
- (기존 사양) 스위치 캡쳐사진 업로드 → EasyOCR 서버가 포켓몬 이름·기술·노력치 자동 인식, learnset 기반 필터링, 빈값 fallback, 이미지 회전, 노력치 합계 표시.

### 3-4. 대미지 계산기
- Gen 9 규칙 적용
- 공격자 / 수비자 포켓몬 선택, SP 배분, 성격, 랭크 보정, 도구
- 날씨 보정: 쾌청·비·모래바람·설경
- 필드 보정: 그래스·미스트·사이코·일렉트릭 (타입 강화 + 미스트의 드래곤타입기술 약화, 그래스의 지진·땅고르기 약화)
- 벽 보정: 리플렉터·빛의장막·오로라베일 (급소 시 무효)
- 솔라빔·솔라블레이드: 쾌청 이외 날씨 위력 절반
- 무조건 급소 기술 자동 체크
- 특성 보정: 약 60종 이상 반영 (특성 이름은 DB 포켓몬 데이터에서 가져옴)
- 멀티스케일: 수비자 특성으로 멀티스케일 선택 시 체크박스 표시 → 활성화하면 받는 데미지 ×0.5 계산
- **보정값 칩 표시**: 계산 결과에 적용된 모든 보정(자속보정·타입상성·날씨·필드·벽·급소·멀티스케일 등)을 컬러 칩(pill)으로 표시 — 강화(배율 >1) emerald, 약화(배율 <1) orange
- 탁쳐서떨구기: 수비자가 도구를 지니고 있으면 위력 ×1.5 (수비자 도구 선택 시 자동 적용)
- 바디프레스: 공격자 방어 수치로 계산
- 지닌도구 지원, 공격자/수비자 스왑 시 도구 교환
- Combobox 화살표 키 네비게이션
- 샘플 불러오기로 등록된 샘플의 스탯 자동 적용
- 실수치 공식: HP = 종족치+75+SP, 나머지 = floor((종족치+20+SP) × 성격보정)
- **수비자 UI 아이템/특성 순서**: 아이템 먼저, 특성 나중 표시
- **일본어 모드에서 특성명**: `/api/data/abilities` API에서 ja 필드를 가져와 일본어로 표시
- 기술·도구·포켓몬 명칭은 모두 DB 데이터 기준으로만 사용 (하드코딩 금지)
- **아이템 위력 보정**: 18종 타입 부스트 도구(목탄·신비의물방울·자석·기적의씨·녹지않는얼음·검은띠·독바늘·부드러운모래·예리한부리·휘어진스푼·은빛가루·딱딱한돌·저주의부적·용의이빨·검은안경·금속코트·요정의깃털·실크스카프) 동일 타입 기술 ×1.2 반영, 계산 결과에 도구 라벨 독립 표시
- **전기구슬**: 피카츄 소지 시 공격/특공 ×2 자동 반영
- **결정력(ketteiryoku) 집계**: 공격(특공) 실수치 × 기술위력 × 특성보정 × 공격자측 버프 전부. 버프 요소 — 날씨(쾌청+불꽃/비+물 ×1.5), 필드(일렉트릭/사이코/그래스 ×1.3), 충전 ×2.0, 도우미 ×1.5, 급소 ×1.5, 타입 부스트 도구 ×1.2, 탁쳐서떨구기 ×1.5. 자속(STAB)은 별도 수치로 표기(적응력 특성 시 ×2.0). 반감 요소는 결정력 계산 제외
- **공격자만 결정력**: 수비자 미선택 상태에서도 공격자+기술+조건만으로 결정력 숫자 즉시 표시 (탁쳐서떨구기 제외, 그 외 버프는 모두 반영)

### 3-5. 사용률 통계
- 샘플 등록/수정/삭제 시 자동 집계 (`UsageStat` 테이블)
- countBased(샘플 수) / weightedBased(좋아요 수) 두 가지 기준
- 포켓몬별 인기 기술·도구·특성·노력치 Top 3 표시
- 코어 조합(자주 함께 쓰는 포켓몬) 분석

### 3-6. 패치노트 시스템
- **PatchNote 모델**: `title`, `content`, `category(String?)`, `createdAt`
- **카테고리**: `"feature"` (기능추가 · 초록) / `"bug"` (버그수정 · 빨강) / `null` (미분류 · 기본색)
- **메인 패널** (`PatchNotePanel`): 최신 5개 표시, 하단 "전체 패치노트 보기 →" 링크
- **전체 패치노트 페이지** (`/patch-notes`): 아코디언 형식, 제목(좌) + 날짜·화살표(우), 1줄 설명은 Y축 중앙정렬, 10개씩 페이지네이션
- **캐시**: `Cache-Control: no-store` — 관리자 수정 즉시 반영 필수
- **관리자 UI** (`PatchNoteManager`): 카테고리 토글 버튼(기능추가/버그수정/선택안함)으로 등록·수정 가능

### 3-7. 버그 신고 시스템
- 신고 카테고리: 포켓몬 정보 오류·기술 정보 오류·도구 오류·계산기 오류·페이지 오류·비속어 신고
- 비로그인·로그인 모두 신고 가능, 한국어 비속어 필터 적용
- 관리자 처리 상태: open / 처리중 / 완료 / 보류

---

## 4. 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js 16 (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS (zinc 계열 다크모드) |
| DB | Neon PostgreSQL (pokenova와 공유) |
| ORM | Prisma |
| 인증 | NextAuth.js (Google OAuth) |
| 배포 | Vercel |
| OCR 서버 | Python FastAPI + EasyOCR (Railway, **2026-05-11 비활성화**) |

---

## 5. 데이터 모델 요약

```
User ──────────────────────────────────────────────
 ├── Team (팀) ──▶ TeamSample[] ──▶ StandaloneSample
 ├── StandaloneSample (독립 샘플)
 ├── TeamLike / StandaloneSampleLike
 └── 닉네임, 관리자 여부

PokemonData ──────── move, item, ability, learnset
UsageStat ─────────── 포켓몬별 집계 데이터
PatchNote ─────────── 관리자 패치노트
BugReport ─────────── 버그/오류 신고 (category, status, title, content)
```

---

## 6. 페이지 구조

| 경로 | 설명 |
|---|---|
| `/` | 메인 (샘플 Top, 최신 팀) |
| `/samples` | 샘플 목록 (검색·필터·정렬) |
| `/samples/new` | 샘플 등록 (OCR은 비활성화, 수동 입력만) |
| `/samples/[id]` | 샘플 상세 |
| `/samples/[id]/edit` | 샘플 수정 |
| `/teams` | 팀 목록 |
| `/teams/new` | 팀 등록 |
| `/teams/[id]` | 팀 상세 |
| `/calc` | 대미지 계산기 |
| `/patch-notes` | 전체 패치노트 (아코디언·페이지네이션) |
| `/stats` | 포켓몬 사용률 통계 |
| `/stats/[id]` | 포켓몬 통계 상세 |
| `/my` | 마이페이지 |
| `/report` | 버그/오류 신고 |
| `/admin` | 관리자 (패치노트 관리, 전체 유저 목록, 통계 집계, 신고 처리) |
| `/setup-nickname` | 닉네임 설정 (최초 로그인 시) |
| `/sign-in` | 로그인 |
| `/users/[id]` | 유저 프로필 |

---

## 7. UI 디자인 원칙

- **배경:** `#09090b` (zinc-950), 카드: zinc-900, 테두리: zinc-800
- **포켓몬 타입별 카드 테두리 색상** — 첫 번째 타입 색 적용
- **계산기:** 공격 패널 빨강(`border-red-500/40`), 수비 패널 파랑(`border-blue-500/40`)
- **노력치 슬라이더:** 공격 계열 빨강, 수비 계열 파랑 accent
- **성격 그리드:** 5×5 table-fixed, 성실(중립)은 대각선에 별도 표시
- **모바일:** 햄버거 메뉴 (오른쪽 슬라이드), input 포커스 시 자동 확대 방지
- **브랜딩:**
  - 파비콘: `MEGA_SIMBOL.webp` (`public/MEGA_SIMBOL.webp`)
  - 네비게이션 바: 타이틀 왼쪽에 MEGA_SIMBOL 로고 이미지 (데스크탑 28px, 모바일 24px)
- **아바타:** Google OAuth 프로필 이미지 — 네비바·등록자 표시·관리자 유저 목록에 사용
- **메인 페이지 샘플 섹션**: 섹션 헤더 우측에 "당신의 샘플을 공유하세요" 문구 + "+ 샘플 추가" indigo 버튼 → `/samples/new`
- **OG / SNS 공유 메타태그 (영문):**
  - title: `"Pochams — Pokémon Champions Sample & Calculator"`
  - description: `"Pochams — Pokémon Champions Sample & Calculator | 포켓몬 챔피언스 샘플 공유 & 계산기"`
  - OG 이미지: "Pochams" 대형 텍스트 + "Pokémon Champions Sample & Calculator" 부제
  - X(트위터) 등 SNS 공유 시 영문 우선 표시

---

## 8. SEO / 분석

- **Google Analytics 4:** G-B03JQ26Q27 연결
- **Google Search Console:** 소유권 인증 완료, sitemap.xml 제출
- **네이버 서치어드바이저:** 소유권 인증 완료, sitemap.xml 제출
- **OG 이미지:** "Pochams" 영문 텍스트 기반 1200×630 이미지 (공유 미리보기용, next/og ImageResponse)
- **sitemap.xml:** `/`, `/teams`, `/samples`, `/calc`, `/stats` 포함
- **Canonical URL:** root layout에 `metadataBase: https://pochams.com`만 박고 canonical은 페이지별로 명시 (Next.js metadata는 shallow merge라 root에 canonical을 박으면 자식 미명시 페이지가 전부 상속받아 홈 중복으로 판정됨). 모든 공개 페이지(`/`, `/teams`, `/samples`, `/calc`, `/stats`, `/pokedex`, `/patch-notes`)에 `alternates.canonical: '/<path>'` 명시. 동적 상세 페이지(`/samples/[id]`, `/teams/[id]`, `/stats/[id]`)는 generateMetadata로 self-canonical 동적 생성. patch-notes는 client component라 부모 layout.tsx 신설해서 처리
- **색인 차단:** `robots.ts` disallow + 각 server component 페이지 `metadata.robots: { index: false, follow: false }` 이중 방어. 차단 대상은 `/admin`, `/api/`, `/sign-in`, `/my`, `/setup-nickname`, `/report`, `/users/`, `/samples/new`, `/samples/*/edit`, `/teams/new`, `/teams/*/edit`

---

## 9. 개발 히스토리 요약 (주요 마일스톤)

| 날짜 | 내용 |
|---|---|
| 2026-04-09 | 프로젝트 초기 세팅, DB 스키마, 포켓몬 데이터 연동 |
| 2026-04-10 | 팀/샘플 CRUD, 마이페이지, 통계, 메가진화 계산기 |
| 2026-04-11 | OCR 샘플 등록, 계산기 UX 개선, 비로그인 등록 허용 |
| 2026-04-12 | 통계 집계 API, OCR 개선, zinc 다크모드 전환 |
| 2026-04-13 (전반) | 계산기 날씨·벽·급소 규칙, Combobox 키보드 내비게이션, 성격 UI |
| 2026-04-13 (후반) | 버그 신고 시스템, GA4·SEO·OG 이미지, 모바일 최적화, OCR 다수 개선 |
| 2026-04-14 (전반) | 샘플/팀 싱글·더블 포맷 필터 추가, API 코드 최적화, 복수 버그 수정 |
| 2026-04-14 (후반) | 계산기 실수치 공식 버그 수정, 특성·필드 효과 버그 수정, DB 명칭 규칙 정립 |
| 2026-04-15 (전반) | Vercel 리전 sin1(싱가포르), CDN 캐시, 기술·도구 한/영 표시, 댓글·아바타, OCR 드롭다운 개선, 샘플 상세 레이아웃 |
| 2026-04-15 (후반) | 샘플 이름 suffix 고정 UX + 영어 모드 `{영어이름} Set` 전체 적용 + 기존 DB 샘플 이름 정규화 + 파비콘·네비바 로고(MEGA_SIMBOL) + 한/영 언어 전환 main 머지·배포 완료 + OG 메타태그 한영 병기 |
| 2026-04-16 (전반) | 영어 버전 통계 한국어 잔재 수정 — SpTop3 엣지케이스 + StatsRow 기술/도구/특성/샘플 이름 GameNamesContext 번역 적용 |
| 2026-04-16 (후반-1) | 대미지 계산기 멀티스케일 적용 버튼 추가 — 수비자 특성 선택 시 체크박스 자동 노출, 활성화 시 받는 데미지 ×0.5 |
| 2026-04-16 (후반-2) | 계산 결과 보정값 칩 표시 (자속/타입상성/날씨/필드/벽/급소/멀티스케일 등 emerald·orange 컬러 칩) |
| 2026-04-16 (후반-3) | 패치노트 전용 페이지(/patch-notes) 신설 — 아코디언·페이지네이션, 헤더 nav 추가, 메인 패널 5개 제한 + 전체보기 링크 |
| 2026-04-16 (후반-4) | 패치노트 카테고리 기능 — PatchNote.category(feature/bug) 스키마 추가, 관리자 UI 토글 버튼, 패치노트 제목 색상 구분(초록/빨강), CDN 캐시 no-store 수정 |
| 2026-04-16 (후반-5) | 관리자 페이지 전체 유저 목록 추가 — 프로필 사진·검색·페이지네이션, 최근 가입 유저에도 아바타 표시 |
| 2026-04-16 (후반-6) | 메인 페이지 단일 샘플 섹션 헤더에 "당신의 샘플을 공유하세요" 문구 + "+ 샘플 추가" 버튼 추가 |
| 2026-04-16 (후반-7) | OG 이미지·메타데이터 영문화 — title/description/og-image 모두 영문("Pochams — Pokémon Champions Sample & Calculator")으로 변경 |
| 2026-04-16 (후반-8) | 코드베이스 오류 수정 및 최적화 — isAdmin 공통 유틸(lib/isAdmin.ts) 중앙화, 팀 수정 API $transaction 원자화, fetch 에러 핸들링 추가 |
| 2026-04-16 (후반-9) | 팀 댓글 기능 완성 — TeamCard 댓글 수 표시, getTeamList에 _count.comments 포함 |
| 2026-04-16 (후반-10) | 도감 메가플라엣테 누락 버그 수정 — CHAMPIONS_MEGA_ONLY_ENS 도입, getPokedexEntries 메가 쿼리 inChampions 직접 필터로 개선 |
| 2026-04-16 (후반-11) | 팀 댓글 익명 팀 차단 버그 수정 — team.authorId 조건 제거, 모든 팀에 댓글 허용 |
| 2026-04-18 | 비로그인 팀 작성자 anonName 표시, 관리자 닉네임 미설정 뱃지, 메인 샘플 추가 버튼 항상 표시, 포켓몬 API formLabel/regionType 자동 주입 |
| 2026-04-19 (전반) | 계산기 탁쳐서떨구기 도구 소지 시 위력 ×1.5 적용, 도메인 pochams.com 연결 완료 처리, KO/JA/EN 3개 언어 지원 완성 — 계산기·도감·샘플 전체 일본어화, Prisma ja 필드 + PokeAPI 연동 |
| 2026-04-19 (후반) | 일본어 2차 현지화 완성 — T컴포넌트 ja prop 추가, 메인/팀목록·상세/샘플목록/통계목록·상세/계산기/오류신고/패치노트 전 페이지 일본어화, BATTLE_FORMAT_LABEL_JA 추가, 패치노트 일본어 선택 시 영어 버전 표시 |
| 2026-04-19 (3차) | 일본어 3차 현지화 완성 — SampleNewForm/SampleCard/TeamCard/PokemonSlotForm/MySamplesModal/setup-nickname/팀 edit 클라이언트 전부 일본어화, suffix 'サンプル' 버그 수정, 전체 UI 일본어 지원 완료 |
| 2026-04-20 | 계산기 개선 — 수비자 메가진화 시 메가스톤·특성 자동 적용, 아이템/특성 순서 변경, alt폼 스탯 오류 수정(id < 100000 필터), 계산기 일본어 특성명 API 연동, upsert 스크립트 추가(npm run upsert) |
| 2026-04-21 | 세레비넷 비교 후 누락 기술 683개 추가(185마리 전수), learnsets API 캐시 3600→300초 단축, 세레비넷 learnset 비교 가이드 문서화, 팀 상세/작성 폼 메가폼 자동 표시·전환 수정, dead code 3개 파일 제거 |
| 2026-04-24 | 계산기 결정력 표시값 특성 보정 미반영 버그 수정(effAtk×power×atkMod.multiplier로 변경) — 철주먹/예리함/메가런처 등 위력 배율 특성이 결정력에 반영되도록 수정, 페어리스킨/프리즈스킨/스카이스킨 3종 특성 추가(노말 기술 ×1.2) |
| 2026-04-25 | 메가플라엣테 누락 복구 — DB inChampions stale 문제로 계산기에서 미노출, `npm run upsert pokemon floette`로 base/메가/영원의꽃 재반영. 추가로 getTagPokemonOptions 비대칭 필터 버그 수정(메가 row의 baseIds 의존 제거, 알트폼과 동일하게 inChampions 직접 필터로 통일) — 샘플 파트너/카운터 태그에서도 메가플라엣테 노출. /api/data/pokemon 라우트에도 동일한 비대칭 버그 수정(계산기 ?championsOnly=true&includeMega=true 응답에서 메가플라엣테 누락 근본 원인). 도감 메가 카드 클릭 시 popupId를 entry.id 그대로 사용해 메가별 통계 팝업 표시(기존엔 강제 base id 변환으로 메가눈여아 → 눈여아 통계가 뜸) |
| 2026-05-08 | SEO — Search Console "사용자가 선택한 표준이 없는 중복 페이지" 경고 대응. (1차) root layout에 metadataBase + alternates.canonical 박고 sitemap 등록 4개 페이지(/teams /samples /calc /stats)에 page-specific canonical 명시. /admin /my /users /samples/new /teams/new /teams/*/edit에 metadata.robots noindex + robots.ts disallow 이중 차단. client component 페이지(/sign-in /setup-nickname /report /samples/[id]/edit)는 robots.ts disallow로만 처리. (2차) Next.js metadata shallow merge로 root canonical이 자식에 상속되는 cascade 버그 발견 — root에서 canonical 제거하고 모든 공개 페이지(홈 / pokedex / patch-notes / samples/[id] / teams/[id] / stats/[id])에 명시적 canonical 추가. patch-notes는 client component라 layout.tsx 신설 |
| 2026-05-11 | OCR 기능 비활성화 — Railway 무료 트라이얼 만료(2026-05-12)로 EasyOCR 서버 운영 중단. `SampleNewForm.tsx`에서 OCR 버튼·다이얼로그·`SampleOCR` 임포트·`handleOCRFill`·`showOCR` 상태 제거, `/api/ocr/route.ts`를 503 응답으로 단축. `ocr_server/` 폴더와 `OCR_SERVER_URL` env 변수는 그대로 둬서 다른 PaaS로 옮기면 즉시 복구 가능 |
| 2026-06-24 | 계산기 내던지기(fling) 도구별 위력 자동 계산 — `power:null`이라 기존 기술목록 필터(`m.power !== null`)에서 빠져 선택조차 안 되던 내던지기를 목록에 포함. 지닌 도구에 따라 위력 자동 결정(`FLING_POWER` 챔피언스 도구 72종, 출처 PokeAPI `fling_power` — 검은철구 130·딱딱한돌 100·생명의구슬 30·베리류 10 등). 도구 선택/변경 시 위력 자동 세팅(타입 악·물리 고정), 내던질 수 없는 도구(메가스톤·요정의깃털)·무도구는 발동 안 함(위력 0→결과 미표시). 위력란은 내던지기일 때 읽기전용+안내 문구. 도구를 던지는 기술이라 그 도구의 데미지 보정(생명의구슬 ×1.3·타입부스트 ×1.2 등)은 미적용(결정력·실데미지 두 블록 itemMod 무효화로 이중 적용 방지). lint 0·verify_calc·build 통과 |
| 2026-06-24 | 계산기 변환자재/리베로 자속 토글 추가 — 변환자재(protean)·리베로(libero) 보유 포켓몬 선택 시 '특성·도구 효과' 칸에 on/off 토글 노출. ON이면 모든 기술을 자속으로 간주해 데미지 ×1.5 적용(`isStab`을 강제 true). 원래 자속 기술은 이미 isStab=true라 중복 없이 1.5배 한 번만 적용. 결정력·실데미지 두 블록 동시 반영, `DamageCalc.tsx` 단일 파일 수정, lint 0·verify_calc·build 통과 |
| 2026-06-23 | 계산기 반감열매 버그 수정 — 오카열매류 type-protection 18종 중 초나열매(wacan)·린드열매(rindo)의 반감 타입이 서로 뒤바뀐 오류 교정. `data/quiz_items.json` desc(초나=전기/린드=풀) + `DamageCalc.tsx` BERRY_RESIST 동일 수정, verify_calc·build·lint 0 통과. 검증 중 `upsert.ts` 독립 누락 버그 발견 — DATA_DIR이 `pokenova_project/data`, SPRITE_BASE가 `pokenova.vercel.app`을 가리켜 seed.ts와 다른 소스를 읽고 있었음. `../data`·`""`로 교정해 단건 upsert가 옛 데이터·pokenova 절대 sprite URL을 덮어쓰던 문제 해소, DB 전수 오염 0 검증 |
| 2026-06-20 | 데이터 동기화 브릿지 구축 — 시즌 데이터 추가를 "JSON 한 곳 수정 + `npm run sync` 한 명령"으로 포챔스(DB seed)·포케노바(static push) 동시 반영. 챔피언스 포켓몬 목록을 `champions_pokemon_ids.json` 단일 원천으로 통합(seed.ts·재생성 스크립트 공유), seed.ts 하드코딩 ID 3종 제거. 인라인 파이썬을 `scripts/regen_champions_learnsets.py`로 분리하며 seed.ts vs CLAUDE.md 간 670 불일치 정리(altOnly/megaOnly 원종 ID 자동 유도로 양쪽 동작 보존). 재생성 결과 기존과 완전 동일 검증(186마리·11542기술) |
| 2026-07-09 | 포챔스 반영 파이프라인 완성 — 로스터 단일 원천을 `champions_pokemon_ids.json` → 포케노바 `pokedex.json`의 `inChampions` 플래그로 이동. 브릿지 `champions_from_pokedex.py` 신설(플래그→ids 자동 계산, floette 폼 단독 챔피언 특수케이스 포함), `sync-data.sh`를 0~5단계로 확장(브릿지·위키수집·소스생성·seed). 라운드트립 로스터 완전 일치 검증(205+1+1) |
| 2026-06-20 | 시즌3(레귤레이션 M-B) 데이터 반영 — 포켓몬 20종(일반 19 + 찌르호크) 챔피언스 추가, 기술 5종·도구 31종(도구 15 + 메가스톤 16) in_champions ON. 방침: md를 정답지로 포켓몬별 `learnsets.json` 기술폭을 통째 교체(프론트가 learnset의 en만 사용 확인). md에 없던 찌르호크 기술폭은 pokemon.fandom.com API로 보강. 원본 md는 `docs/champions_seasons/`에 시즌별 보관(README 추적 인덱스). 챔피언스 포켓몬 186→206마리. ※ 기술 위력/PP 스탯 동기화는 계산기 영향으로 후속 과제 |
| 2026-06-20 | **포챔스 전용 DB 구축** — 포케노바 공유데이터와 분리한 자체 데이터(`pochams_project/data/`) 신설. 포켓몬위키 `Champions 기술` 문서를 챔피언스 205종 전부 수집해 `pochams_pokemon.json`(이름·종족값·타입·특성·기술폭) / `pochams_moves.json`(분류·카테고리배열·위력·명중·우선도·접촉·효과) / `pochams_abilities.json`(이름·효과) 생성. 도구는 기존 챔피언스 도구 재사용. 위키(기술폭·챔피언스 스탯)+기존데이터(접촉/효과/종족값/특성효과) 하이브리드, 미매칭 0. `scripts/build_pochams_db.py`. ※ Neon 적재·사이트 연동은 다음 단계 |
| 2026-06-20 | **대규모 개편 — 레퍼런스 도구로 전환.** 샘플 등록·팀·상세 사용률(/stats)·마이페이지·유저·공개 로그인/회원가입 전부 제거. 도감·대미지 계산기·주간 사용률 Top10(홈, admin 수동 입력)·버그신고·패치노트만 유지. 관리자 인증은 Google+ADMIN_EMAIL 유지(공개 로그인 없음, /sign-in은 관리자 전용). DB에서 회원·샘플·팀·UsageStat 삭제(백업 보관), 마스터·주간랭킹·패치노트·버그신고 유지. 계산기 '내 샘플 불러오기/인기기술' 제거, 도감 통계 팝업 제거. Prisma 미사용 모델은 마이그레이션 리스크로 데이터만 비우고 정의는 유지 |
| 2026-06-21 | **리팩터 잔재 미사용 코드 일괄 정리(−2132줄).** Explore 에이전트로 참조 0건 확인 후 3개 등급 제거. (A) 죽은 코드 — `queries.ts`(팀·샘플·통계 함수 10개)·`aggregateStats.ts` 파일 전체, 미사용 shared `NatureSelector.tsx`, `buildSampleDisplayName` + stale import. (B) 공개 화면 없는 기능 전부 제거 — **주간랭킹·패치노트·GameNamesContext**(모델·API·관리자 매니저·layout provider·admin 섹션). admin은 버그신고만 유지. (C) 비코드 잔재 — OCR 인프라(`ocr/`·`ocr_server/`·`public/ocr/`·`kor.traineddata`) + `tesseract.js` 의존성, 중복 `.env.vercel.*` 5개, 일회성 마이그레이션 스크립트 4개(backups/ 보존). Prisma 모델 11개 + `BattleFormat` enum 제거 후 `db push`(patch_notes·weekly_rankings 테이블 drop). 빌드 통과. **이어서 비계산기 lint 에러 정리** — seed.ts/upsert.ts의 `any` 27개를 소스 JSON 인터페이스 + `readJson` 제네릭 + `Prisma.PokemonDataCreateManyInput`로 제거, gameData 미사용 `baseIds` 제거(에러 51→24). 남은 24개는 계산기(DamageCalc) React 훅 규칙으로, **계산기를 UI/UX부터 새로 만들 때** 처리 예정(데미지 계산 결과·공식은 현재 값 보존, verify_calc.mjs 통과 유지). ⭐ 다음 작업 = 계산기 재작성 |
| 2026-06-21 | **계산기 재디자인 + 데미지 영향 도구·특성 확장.** (1) 재디자인 — 계산 엔진 그대로 두고 화면만 배틀 HUD 다크 테마로(Oxanium 폰트·공격 로즈/수비 스카이 카드·대형 데미지 readout+HP게이지·KO 배지·흰 배경 버그 수정), CheckBox 분리로 static-components 11개 해소, 헤더 아이콘 제거·'기술&조건'→'기술'. (2) 도구 추가 — 힘의머리띠 물리×1.1·박식안경 특수×1.1·달인의띠 효과굉장×1.2·타입반감열매 18종(수비자 ×0.5)·메트로놈 0~5(×1.0~2.0). (3) 특성 추가 — 우격다짐×1.3·의욕 물리×1.5·선파워 쾌청 특공×1.5·근성 토글×1.5·총대장 0/1/2. '특성·도구 효과' 활성화 칸 신설. (4) 스피드 실수치 반영 — 구애스카프×1.5·날씨/필드 특성×2·트리거형 토글. 결정력·실데미지 두 블록 일관 반영, verify_calc·build 통과. (5) 수비 특성 3종 추가(퍼코트·정화의소금·건조피부) + 계산기 React 훅 lint 13개 정리(set-state-in-effect 의도적 disable, useMemo 누락 의존성 보강으로 staleness 버그까지 해소) → 세션 시작 51개에서 **lint 0 problems** 달성 |
| 2026-06-21 | **도구·특성 도감 신설 + 영/일 임시 비활성.** `/items` 도구 도감(이미지+이름+효과, 검색·카테고리 필터, 챔피언스 148) · `/abilities` 특성 도감(이름+설명+보유 포켓몬, 검색·초성 ㄱㄴㄷ 필터, 챔피언스 194). 네비게이션 = 도감·**도구·특성**·계산기·신고. 도구 이미지 148개를 포켓몬위키에서 재수집·교체(`scripts/fetch_item_images.py`, 아이콘 9세대/ZA 파일 직접 탐색, WebP→PNG 변환). 영어·일본어는 번역 미완으로 `LANG_LOCKED`(LangContext) 플래그로 임시 비활성(한국어 고정, 토글 숨김, false로 복구). build·lint 0 통과 |
| 2026-07-11 | **영어/일본어 언어 잠금 해제.** `LANG_LOCKED` true→false로 헤더 KO/JA/EN 토글 + 브라우저 언어 자동감지 재활성화(영어권 자동 EN, 한국어권 KO 유지). 남은 하드코딩 정리 — `loading.tsx` '불러오는 중...'→'Loading…', `global-error.tsx` 한국어→영어(lang ko→en). 조사 결과 사전(i18n.ts 144키 완비)·포켓몬 이름/타입(`fmtName`·`TYPE_EN`)·도감/도구/특성/계산기/신고 UI·report 카테고리는 이미 EN 대응 완료 상태. 메타데이터(탭 제목/description)는 apex 한국어 canonical SEO 보호 위해 한국어 유지. typecheck·build 통과 |
| 2026-07-12 | **인프라 — DNS Cloudflare 이전 · Vercel Pro 복구 · Firewall 봇 차단(대시보드, 코드 커밋 없음).** 트래픽 폭증(사후 확인: 15h 전 160만 건 DDoS)으로 pochams 일시정지 → 복구. (1) DNS 관리처를 가비아→Cloudflare 네임서버(`april/elliot.ns.cloudflare.com`)로 이전, 가비아는 소유권만 유지. pochams.com A/www는 DNS only(회색)로 Vercel 직결 유지. (2) Vercel Pro 업그레이드로 일시정지 해제(200 복구), Spend Management On-Demand Budget $30·Pause=Off. (3) Vercel Firewall 봇 차단 — Bot Protection=Challenge(브라우저·검증봇 통과/정체불명 봇 챌린지), AI Bots=Deny(AI 크롤러 차단), DDoS Mitigation 자동 유지. 되돌리기는 각 드롭다운 Off. 정적·고트래픽인 포케노바는 Cloudflare Workers로 분리해 Pro 대역폭 비용 격리. (보류) Cloudflare Proxy 전환 — SSL Full strict 선행 후 다음 세션. 커밋: 484299a(sync-data.sh 배포처 Vercel→Cloudflare 주석) |
| 2026-09-02 | **Z 메가진화 3종 특성 공개 반영 + 계산기 수비 특성 2종 추가.** 2026-08-30 공식 발표(Regulation Set M-C, 9/9 시작)로 미공개였던 Z 메가 특성이 나왔다 — 메가앱솔 Z=예리함(`sharpness`), 메가한카리아스 Z=부유(`levitate`), 메가루카리오 Z=**파동의방호**(`aura_guard`·`はどうのぼうご`, 신규). (1) 계산기 `calcDefenderAbilityMod`에 `aura_guard`(접촉기 ×0.5, 푹신푹신에서 불꽃 2배만 뺀 형태) 추가. 수비 특성은 이 함수 한 곳만 고치면 결정력·실데미지 두 블록에 동시 반영된다(공격 특성·도구와 달리 복붙 불필요). (2) `levitate`(땅 기술 ×0) 동시 추가 — **계산기에 부유가 아예 없어서** 또도가스·무우마 등 기존 부유 포켓몬도 땅 기술을 그대로 맞고 있었다(기존 결과값 변동 있음). (3) 특성 원천이 `pokedex.json`이 아니라 **`quiz_pokemon.json`**임을 확인 — `pokedex.json`은 `build_pokedex.js` 생성물이고 재생성 시 `inChampions`·`shinySprite`만 보존된다. CLAUDE.md에 "포켓몬 속성 — 단일 원천" 절 신설. (4) 데이터 재생성에 포케노바 itemdex 아이콘 교체분이 딸려와 챔피언스 도구 148개 중 **72개 이미지가 깨짐** → 해당 파일을 `public/sprites/items/`로 복사해 자체 호스팅 유지 + Bulbapedia HD 아이콘 확보. lint 0·build·verify_calc 통과, Neon 반영 확인(특성 194→195종). 아직 특성 미공개 11종 잔존(메가라이츄 X·Y, 메가찌르호크, 메가펜드라, 메가곤율거니, 메가저리더프, 메가화염레오, 메가칼라마네로, 메가거북손데스, 메가드래캄, 메가대여르). 커밋 289cccf/1ffa293/e61f68e |
| 2026-09-02 | **미공개 특성 11종 해소 + 기술 `contact` 플래그 72개 교정.** (1) 특성 `?`로 남아 있던 메가 11종이 사실 M-B 때 이미 공개돼 있었다 — Serebii 챔피언스 메가 특성 페이지 + 한국어 포켓몬 위키 + 파미통·GameWith 교차 확인으로 전부 채워 **미공개 특성 0개**. 9종은 기존 특성(일렉트릭메이커·노가드·심술꾸러기×2·조가비갑옷·위협·단단한발톱·재생력·오기)이라 새 이름이 필요 없었고, 신규는 **천정부지**(`eelevate`·`うなぎのぼり`, id 313, 부유+자기과신)와 **불꽃의갈기**(`fire_mane`·`ほのおのたてがみ`, id 314, 불꽃기 위력 1.5배) 둘뿐. 계산기 추가도 둘뿐 — `fire_mane`(공격, 불꽃기 ×1.5)·`eelevate`(수비, 땅기 ×0. 데미지 영향은 땅 무효뿐이라 `levitate`와 같은 case). 나머지 7종은 랭크·명중·급소·회복이라 배율 로직 대상 아님. (2) **기술 `contact` 플래그 72개 교정** — 파동의방호 검증 중 발견. 스톤에지·사이코커터·폴터가이스트·에어슬래시가 접촉으로, 성스러운칼·기선제압·헤비봄버·아이스해머 등이 비접촉으로 잘못 들어가 있었다(929개 중 72개, 챔피언스 37개). **파동의방호뿐 아니라 기존 `단단한발톱`(×1.3)·`푹신푹신`(×0.5) 계산도 이미 같은 37개에서 틀리고 있었다.** 기준 소스는 Pokémon Showdown `flags.contact`(표본 7개를 Bulbapedia "Makes contact"와 대조해 채택, 미매칭 0). 재발 방지용 `pokenova_project/scripts/sync_move_contact.js` 신설(`--check`는 차이만 보고·exit 1). lint 0·build·verify_calc 통과, DB 특성 195→198종. 커밋 03fd75c |
| 2026-09-10 | **items·abilities 색인 확인 완료(예약 항목 종료) + 레귤레이션 M-C 신규 로스터 사전 조사.** (1) 2026-07-04에 넣은 색인 생성 요청이 정상 처리됐음을 Search Console URL 검사(GOOGLE 색인 탭)로 확인 — `/items`·`/abilities` 둘 다 "페이지 색인이 생성됨". `/`·`/calc` 포함 주요 4개 페이지 전부 색인 완료. **7/12에 켠 Vercel Bot Protection(Challenge)은 Googlebot 색인에 영향이 없었다**(외부 curl은 robots.txt·sitemap.xml까지 429 `Vercel Security Checkpoint`를 받지만 검증된 봇은 통과). 검사 중 만난 "속성에 URL이 없음"은 소유권 문제가 아니라 속성 4개 중 `pokenova.pochams.com`이 선택돼 있어서였다 — RESUME에 함정으로 기록. 로컬 SEO 요소(sitemap·robots·canonical·SSR 본문·내부 링크)도 전수 점검해 이상 없음. (2) **레귤레이션 M-C(9/9 적용) 사전 조사, 데이터 변경은 아직 없음.** 메가 6종 중 Z 3종은 9/2에 반영 완료, **메가보만다**(스카이스킨)는 데이터에 이미 있어 `inChampions`만 켜면 된다. **메가갑주무사·메가드닐레이브는 특성 미공개** — 포켓몬 위키의 `특성1=` 칸이 비어 있고, 영어권 매체는 메가진화 전 원래 특성(긴급탈출·아이스바디)을 적어놔 신뢰할 수 없어 추측 입력하지 않고 `?`로 유지. **신규 포켓몬 23종은 전부 `pokedex.json`에 존재하고 `inChampions=false`** 상태지만, **위키에 `{이름}_(포켓몬)/Champions_기술` 문서가 하나도 없어**(대조군 한카리아스는 기술 58개 정상) 지금 `npm run sync`를 돌리면 전원 기술폭 0으로 도감에 올라간다 → 실행 보류. 매체별 로스터 대조에서 **빠르모트** 1종이 Game8에만 있어 공식 24종과 1종 차이가 남 — 게임 내 입국 목록으로 확정 필요. 커밋 ec55d9e |
| 2026-09-10 | **도감 기술폭 팝업 + 데이터 보정 경로(`overrides.json`) 신설.** (1) 도감(`/`) 카드를 누르면 그 포켓몬의 **챔피언스 기술폭 팝업**이 열린다 — 타입·분류·위력·명중·접촉 표시, 분류 필터(전체/물리/특수/변화)와 위력·이름 정렬, ESC·배경 클릭 닫기, 한/영/일 대응. 기술폭은 팝업을 열 때만 `GET /api/data/learnset/[en]` 으로 받아오고 한 번 받은 건 캐시한다. `PokedexClient` 가 200줄이라 `PokedexCard` 를 별도 파일로 분리. (2) **데이터 보정 경로 신설** — `learnsets.json`·`quiz_moves.json` 은 위키 수집으로 매번 재생성되는 파일이라 손으로 고치면 다음 sync 때 사라졌다. 이제 **`data/overrides.json`** 하나만 고치면 되고 그 값이 항상 수집본을 이긴다(기술 위력·명중·접촉·챔피언스 포함 여부 / 포켓몬별 기술 remove·add). `scripts/apply_overrides.py`(`npm run overrides`, `--check` 는 검사만)를 `sync-data.sh` 3/6 단계로 편입. **없는 키를 적으면 오류로 멈추고 아무것도 고치지 않는다** — 오타가 조용히 묻히는 걸 막기 위한 설계. (3) 사용자가 게임에서 M-C 입국 목록을 확인해 **신규 메가 2종 특성 확정 — 메가갑주무사=단단한발톱(`tough_claws`), 메가드닐레이브=열교환(`thermal_exchange`)**. 둘 다 이미 있는 특성이라 신규 특성 추가는 없고, 단단한발톱은 계산기에도 이미 들어 있다(열교환은 랭크 상승·화상 무효라 배율 로직 대상 아님). (4) 위키 `Champions 기술` 문서 재확인 — 23종 중 **푸크린 1종만 생성됨**(기술 98개), 나머지는 없어 로스터 sync 보류 유지. lint 0·build·verify_calc 통과, 실제 화면에서 팝업 열기/필터(57→24)/정렬/ESC 확인. 커밋 10ae7b7 |
| 2026-09-10 | **레귤레이션 M-C 반영 — 챔피언스 206 → 230종 + 기술폭 출처 3단화.** (1) **Serebii 챔피언스 도감을 보조 출처로 신설.** 포켓몬위키는 신규 포켓몬 `Champions 기술` 문서가 늦게 올라온다(M-C 23종 중 1종). 푸크린으로 대조해 위키 수집본과 **98:98 완전 일치**를 확인하고 채택했다. 우선순위 **`overrides.json` > 포켓몬위키 > Serebii**, 기술 이름이 슬러그에 안 붙으면 오류로 멈춘다(`scripts/fetch_serebii_learnsets.py`). (2) **조용히 틀리는 버그 5개 교정** — ⓐ 챔피언스는 기술 위력이 본편과 다르다(베어가르기 70→80·노려맞히기 80→85·스타어설트 150→170, 전부 신규 포켓몬이 배우는 기술이라 로스터를 늘리면 계산기가 틀렸다). ⓑ `in_champions` 가 손으로 켜는 플래그라 32개 어긋나 신규 전용기 16개가 팝업·API 에서 사라질 뻔했다 → **기술폭에서 끌어내는 파생값으로 전환**. ⓒ seed 가 "base 가 챔피언이면 폼도 전부 챔피언"으로 추론해 가라르 파오리·가라르 마임맨이 딸려 올라갈 뻔했다 → **`formEns` 단일 원천**. ⓓ 로스터를 켜도 스프라이트가 안 따라와 푸크린이 그림 깨진 채 배포됐다 → `scripts/sync_sprites.py` 를 sync 에 편입. ⓔ 폼 기술폭 보강이 Serebii 반영 전 값을 봐서 신규 폼이 전부 기술 0개였다. (3) **로스터 확정** — 신규 24종(파오리·마임맨 **원종만**, 꿀꺽몬, 페르시온, 보만다, 고고트, 갑주무사, 고릴타, 에이스번, 인텔리레온, 폭슬라이, 스트린더, 케오퍼스, 나이킹, 창파나이트, 찌르성게, 에써르, 빠르모트, 올리르바, 시비꼬, 마피티프, 드닐레이브 + **타부자고·묘두기**=이전 레귤레이션 누락분) + 폼 7개(알로라 페르시온·스트린더 로우한·에써르 암컷·시비꼬 옐로 페더·메가보만다·메가갑주무사·메가드닐레이브). 가라르 파오리·가라르 마임맨·거다이맥스·일반 플라엣테는 제외(인게임 확인). (4) **특성** — 메가갑주무사=**단단한발톱**(계산기에 이미 있음), 메가드닐레이브=**열교환**(배율 대상 아님), 시비꼬는 숨겨진 특성이 그린·블루=근성/옐로·화이트=**우격다짐**으로 갈려 **우격다짐이 계산기 ×1.3** 이라 옐로 페더를 폼으로 신설(능력치·기술폭 동일, 전용 이미지 없어 기본 그림 사용). (5) `npm run sync` 가 **0~8 단계**가 됐다. 도감 시즌 배지 M-B → M-C. lint 0·build·verify_calc 통과, 도감 344장·깨진 이미지 0. 커밋 2d9d9a6/496b05c/97afe05/748f5f3/65547e0/2d34250 |
| 2026-09-10 | **시비꼬 옐로 페더 전용 스프라이트 + 챔피언스 도구 18종 반영 + 계산기 도구 3종 적용.** (1) **깃털색 스프라이트** — 시비꼬 그린과 옐로가 같은 기본 이미지(931)를 써서 도감에서 구분이 안 됐다. 세 출처를 다 확인해 **PokeAPI 스프라이트 저장소**(green 931 / blue 10260 / **yellow 10261** / white 10262)를 골랐다. 지금 파일명 규칙(`master__sprites__pokemon__other__official-artwork__{id}.png`)이 이 저장소 경로를 그대로 옮긴 것이라 출처·크기(475×475)가 기존 카드와 같다. 포켓몬위키에도 같은 공식 아트워크가 570×570으로 있으나 다른 카드가 전부 475라 미채택, Serebii는 SV 박스 아이콘뿐이라 제외. (2) **Serebii 챔피언스 도구 목록과 대조해 꺼져 있던 18종을 켰다 — 148 → 166종.** 데이터·이미지는 포케노바에 이미 다 있었고 `in_champions` 플래그만 안 켜져 있었다. **메가스톤 6종** = 보만다·갑주무사·드닐레이브나이트(M-C 신규 메가 3종)와 앱솔·한카리아스·루카리오나이트Z(9/2에 특성만 넣고 돌은 빠뜨린 것). **이게 꺼져 있어서 계산기가 메가 6종에 메가스톤을 자동으로 못 물리고 있었다**(`getMegaStoneName` 이 만든 이름이 `itemList` 에 없으면 조용히 넘어간다). **지닌 도구 12종** = 풍선·조임밴드·탈출버튼·시드 4종·대파·노말주얼·레드카드·울퉁불퉁멧·그라운드코트. (3) **계산기에 배율이 걸리는 건 3종** — **풍선**(땅 기술 ×0, 수비자 도구. 부유와 같은 처리로 `calcDefItemMod`) · **노말주얼**(노말 기술 ×1.3, `GEM_ITEMS` 표 신설로 다른 주얼이 들어와도 표만 늘리면 된다) · **시드 4종**(해당 필드가 깔리면 방어(사이코·미스트는 특방) **+1랭크**, 기술 분류에 맞는 능력치일 때만·+6에서 자름). 나머지 15종은 급소율(대파)·반동(울퉁불퉁멧)·교체(탈출버튼·레드카드)·지속뎀(조임밴드)·필드 지속(그라운드코트)·메가진화 트리거(메가스톤)라 배율 로직 대상이 아니다. (4) **곁들여 잡은 것 둘** — ⓐ 메가스톤 **5종의 일본어명이 엉뚱한 도구 이름**이었다(갑주무사나이트=`ニビあられ`, 드닐레이브나이트=`まもりのアメ`, 앱솔나이트Z=`なんごくかいがら`, 한카리아스나이트Z=`このはのおてがみ`, 루카리오나이트Z=`ちいさなはなたば`). 포켓몬위키 각 문서 도입부에서 받아 교정. ⓑ **도구 스프라이트를 sync 가 안 채우고 있었다** — 이번 18종이 전부 빠져 있어 그대로 켰으면 도감 이미지가 깨진 채 올라갔다(9/10 푸크린 때와 같은 실수). `scripts/sync_sprites.py` 가 포켓몬뿐 아니라 **도구도 보게** 확장. (5) 로스터 뽑기용 **티켓·쿠폰 34종**(타입별 친화 18·볼 확정 12·기념 로스터·팀메이트·트레이닝·퀵쿠폰)은 Serebii 목록에 있으나 배틀 지닌도구가 아니라 넣지 않기로 결정. `verify_calc.mjs` 에 풍선·시드·노말주얼 시나리오 추가(전부 OK), lint 0·build 통과, 도구 이미지 165개 전부 200, 도감에서 시비꼬 그린/옐로가 서로 다른 그림으로 렌더되는 것 확인. (6) **기존 도구 15종이 도감에서 "기타"로 빠져 있던 것 교정** — 생명의구슬·힘의머리띠·박식안경·달인의띠·메트로놈·검은철구 등 명백한 지닌 도구인데 `champions_category` 가 비어 있었다(`page.tsx` 가 `championsCategory ?? "etc"` 로 떨어뜨린다). 전부 held-items 계열이라 `hold_item` 으로 채웠다. **챔피언스 도구 166종 = 메가스톤 81 · 지닌 도구 57 · 열매 28**, 분류 없는 도구가 0이 돼 "기타" 탭 자체가 사라졌다. (7) **코드 리뷰(`f212fb1..562413e`) 지적 5건 반영** — 전부 재현 확인 후 수정. ⓐ **시드가 접지(grounded) 여부를 안 봤다** — 필드는 땅에 닿은 포켓몬에게만 걸리는데 도구 이름과 필드만 맞으면 무조건 +1랭크를 줘서, 수비자 시비꼬(노말/비행)+그래스시드+그래스필드에 있지도 않은 보정이 걸렸다. `isGrounded`(비행 타입·`levitate`·`eelevate` 제외) 신설. ⓑ **`verify_calc` 가 새 코드를 하나도 안 탔다** — 배율을 손으로 넣고 산술 항등식만 봐서 `GEM_ITEMS` 오타나 미스트시드 `stat` 오기입도 그대로 `OK` 였다. 이제 **`DamageCalc.tsx` 소스에서 표를 직접 긁어** ①게임 규칙 일치 ②도구 이름이 챔피언스에 실재 ③`seedRankBonus` 가 `isGrounded` 를 호출하는지 세 가지를 검사하고, 표를 못 찾으면 멈춘다. 결함 2종(미스트시드 `stat` 오기입·노말주얼 오타)을 실제로 넣어 `exit 1` 로 잡히는 것을 확인했다. ⓒ 시드 검증 허용오차가 경계(차이 정확히 2, 조건 `<= 2`)에 걸려 있어 `getRankMult` 기반 정확한 기대값 `==` 비교로 교체. ⓓ **데미지가 0이어도 배지가 "타입×2 / 4타 이상"** 이라 무효 조합이 "언젠가는 잡힌다"처럼 읽혔다 → `max === 0` 이면 KO 판정·타입 배지 둘 다 **"무효"**(en `No Effect` / ja `効果なし`). 원래 부유에서도 나던 화면이고 풍선으로 경로가 하나 늘어 함께 고쳤다. ⓔ **메가니움(id 154)이 메가진화로 오인됐다** — `getMegaStoneName` 이 이름의 `"메가"` 접두사만 봐서 없는 스톤 `"니움나이트"` 를 만들어냈고, 그 탓에 특성이 `abilities[0]`(심록)으로 강제돼 **숨김 특성 리프가드를 못 골랐고**, 도구를 고른 상태에서 메가니움을 선택하면 도구 콤보박스가 사라지고 "메가스톤 자동" 칩에 엉뚱한 도구 이름이 박혔다. 판정을 **폼 `en` 슬러그(`_mega` / `_mega_x|y|z`) 기준 `isMegaForm`** 으로 교체(메가 폼 80개 전부 일치·base 오인 0건). (8) **계산 공식·도구 보정을 `src/lib/` 로 분리** — 착수 전 체크포인트 태그 `checkpoint-before-lib-split`(커밋 4dd4030) 을 찍고 시작했다. 공식과 도구 보정이 화면 컴포넌트 안에 있어 ⓐ새 도구를 넣으려면 1,895줄짜리 `DamageCalc.tsx` 를 헤집어야 했고 ⓑ검증 스크립트가 공식을 손으로 베껴 갖고 있어 **엔진을 고치면 복사본도 같이 맞춰야 정확**했으며(안 맞추면 조용히 옛 공식으로 통과) ⓒ도구 표는 소스를 글자로 훑어 뽑아내 표 모양이 바뀌면 못 읽었다. **`src/lib/damageFormula.ts`**(실수치·성격 보정·랭크 배율·대미지 범위)와 **`src/lib/itemModifiers.ts`**(도구 표·공격자/수비자 보정·시드 랭크·스피드)로 떼어내고 `DamageCalc.tsx` 는 화면과 입력 상태만 남겼다(**1,895 → 1,660줄**). 특성이 이미 `abilityModifiers.ts` 에 있던 것과 같은 자리다. **검증 스크립트는 이제 이 lib 를 그대로 import 해서 실제 함수를 호출한다** — 복사본도 글자 훑기도 없고, 엔진을 고쳐도 스크립트를 따라 고칠 필요가 없다. 실행이 `node` → `tsx` 로 바뀌어 **`npm run verify`** 를 추가했다(실패 시 `exit 1`). 새로 확인하는 것 — 노말주얼이 노말 기술에만, 풍선이 땅 기술에만 ×0, 반감열매가 효과굉장에서만 ×0.5, 시드 4종이 필드·기술분류·**접지** 세 조건을 다 보는지(비행·부유·천정부지에서 0). 급소·광역기 시나리오도 넣었다(예전 복사본엔 그 축이 아예 없었다). **동작 동일성 확인 2단** — ①옮긴 7개 블록이 체크포인트 시점 원본과 **한 글자도 다르지 않음**을 대조 ②위력·공격·방어·랭크·상성·날씨·화상·도구배율·수비배율·벽 조합 **105,840개를 분리 전 값과 대조해 불일치 0**. 곁들여 아무데서도 안 쓰이던 `src/lib/statCalc.ts`(44줄) 제거(`DamageCalc` 실수치 계산과 같은 공식인데 키 표기만 다른 개편 이전 잔재). `CLAUDE.md` 에 "계산기 구조" 절 신설. 되돌리기 = `git reset --hard checkpoint-before-lib-split`. 커밋 749aed2/5a8e3d1/562413e/4dd4030/48e9b6c (포케노바 4e4c931/1105279) |
| 2026-09-10 | **기술폭이 빈 26종 채움 + 챔피언스 신규 특성 4종 설명 + 일본어 이름 130개 교정.** (1) **`learnsets.json` 에 기술이 하나도 없는 포켓몬이 26종** 있었다 — 배틀팀 빌더·기술 도감에서 그 포켓몬만 기술을 하나도 못 고르는 상태. 원인은 PokeAPI 이름 규칙이다. **대표 폼에 접미사가 붙는다** — 시비꼬는 `squawkabilly` 가 아니라 `squawkabilly-green-plumage`, 불비달마는 `darmanitan-standard`. 옛 수집 스크립트는 404 를 받으면 빈 배열을 넣고 넘어갔다. 이름이 다른 13종은 올바른 슬러그로 재수집, 계절 폼·메가·거다이맥스 13종은 파일 안 원종에서 상속했다(API 재조회는 버전 선택이 달라져 버미루가 7개에서 1개로 줄어든다). **메가냐오닉스·메가화염레오는 PokeAPI 에 새로 생긴 `champions` 버전 그룹에만 있어** 버전 우선순위 맨 끝에 추가. 덤으로 **파오리·마임맨이 가라르 폼 기술을 쓰고 있던 것**도 원종 기술로 교정됐다(base 가 비면 폼 키를 합산하는 `regen_champions_learnsets.py` 규칙 탓). 대응표는 `scripts/fill_missing_learnsets.py`. (2) **챔피언스 신규 특성 4종 설명을 채웠다** — PokeAPI 에 없는 줄 알았는데 이름이 달랐다: 드래곤스킨=`dragonize`, 메가솔라=`mega-sol`, 관통드릴=`piercing-drill`, 하바네로분출=`spicy-spray`. 넷 다 한국어 설명과 뜻이 같은지 대조하고 넣었다(관통드릴 "방어기술을 무시하고 본래 데미지의 4분의1" = "hit even targets that are protecting themselves, dealing 1/4"). **특성 설명 영어 309 → 313(전부) · 일본어 306 → 310.** (3) **일본어 이름 130개 교정.** 요정의깃털 일본어가 `ルガルガンＺ`(루가루암 Z)인 것을 보고 전수 대조했다. 원인은 `fetch_japanese_names.py` 가 `/api/v2/item/{우리 id}` 로 조회한 것 — 우리 번호와 PokeAPI 번호가 다른데 같다고 봤다. **349번 이후 도구가 통째로 밀려** 화석·가면·사탕공예·메가스톤이 전부 엉뚱한 Z크리스탈 이름을 달고 있었다. **도구 124개 · 특성 5개(영문 슬러그가 그대로) · 기술 1개(사이코블레이드 끝 공백)** 교정, 포켓몬 1,025마리는 이상 없음(우리 id 가 곧 전국도감 번호라 무사했다). **챔피언스 메가스톤 34종은 PokeAPI 에 일본어가 없어 포켓몬위키에서 받았다 — 픽시나이트는 `ピクシーナイト` 가 아니라 `ピクシナイト` 다**(규칙으로 지어냈으면 틀렸다). 스크립트는 **이름(슬러그) 기준**으로 다시 썼고 악센트를 떼고 묻는다(`poké_ball` → `poke-ball`, 안 그러면 400). 출처는 **PokeAPI 우선, 없으면 포켓몬위키**, 둘이 어긋나면 PokeAPI 를 쓰고 화면에 남긴다 — 실측 불일치 3건이 전부 위키 오류였다(꿀맛사과 문서에 `みついりりん`, 녹슨방패 문서에 녹슨검 이름). 어느 쪽에도 없으면 **건드리지 않고 보고한다**: 거다이맥스 기술 33종은 PokeAPI 에 항목이 없고 위키에도 기술별 문서가 없어 그대로 뒀다. `--check` 모드로 무엇이 바뀌는지 먼저 보고 저장한다. 함정은 `pokenova_project/CLAUDE.md` 에 기록. lint 0 · build · `npm run verify` 통과 · DB seed 반영. 커밋 4cc2ae1/03bb35e/89bd7aa (포케노바 d0d926b/540e26d/b6f3230/c9f75b4/7682f92) |
| 2026-09-10 | **도구 효과를 코드 표에서 데이터로 옮김 + 미채움 항목 정리.** (1) 도구가 무슨 효과인지가 `src/lib/itemModifiers.ts` 안 표(`TYPE_ITEMS`·`BERRY_RESIST`·`GEM_ITEMS`·`FLOAT_ITEMS`·`SEED_ITEMS`)에 박혀 있었다. 그래서 도구가 늘 때마다 **데이터와 코드 두 곳**을 고쳐야 했고 한쪽만 고치면 조용히 어긋났다. `champions_effect` 필드는 있었지만 166개 중 **30개만** 채워져 있고 모양도 둘(`{type_boost,multiplier}` 18 · `{effect,desc_ko}` 12)로 갈려 있었으며 **코드 어디서도 읽지 않는 죽은 데이터**였다. (2) **챔피언스 도구 166개 전부를 `kind` 로 갈리는 한 모양으로 채웠다** — 계산 대상 49(`type_boost` 18 · `berry_resist` 18 · `seed` 4 · `class_boost` 2 · `gem`·`float`·`flat_boost`·`super_effective_boost`·`metronome`·`speed_boost`·`species_stat_boost` 각 1) · `mega_stone` 81 · `none` 36. 값은 전부 105,840개 조합 대조를 통과한 기존 코드에서 가져와 **새로 지어낸 숫자가 없다.** 기존 `desc_ko` 12개는 `note` 로 보존. (3) **`scripts/build_item_effects.py` 가 데이터에서 `src/lib/itemEffects.ts` 를 생성한다**(`npm run sync` 5/9 단계로 편입, sync 가 0~9 단계가 됐다). 도구 파일 310KB 를 화면 번들에 넣지 않으려고 계산에 쓰는 kind 만 뽑아 8KB 로 만든다 — **빌드 산출물에서 효과표는 들어가고 전체 도구 파일(`desc_lang`)은 안 딸려간 것을 확인**. 모양이 어긋나거나 챔피언스 도구인데 `champions_effect` 가 없으면 **생성이 그 자리에서 멈춘다.** (4) `itemModifiers.ts` 는 표 5개가 사라지고 `kind` 로 갈리는 switch 하나가 됐다. 화면 라벨은 데이터의 `multiplier`·`pokemon_ko`·능력치 이름으로 조립한다. (5) **동작 동일성** — 도구 166개 × 타입 18 × 분류 2 × 상성 6 × 연속스택 5 × 포켓몬 3 + 수비자·시드·스피드 조합까지 **595,614건**을 바꾸기 전 값과 대조해 **불일치 0**(화면 라벨 문자열까지 동일). (6) **`npm run verify` 에 검증 4가지 추가** — ①효과표 개수가 데이터의 계산 대상 수와 같은지 ②값이 원천과 같은지 ③챔피언스 도구가 전부 `champions_effect` 를 갖는지 ④표에 없는 도구에 배율이 안 붙는지. 셋을 일부러 망가뜨려 `exit 1` 로 잡히는 것을 확인했다 — 특히 **데이터만 고치고 sync 를 안 돌린 채 배포하는 사고**가 여기서 잡힌다. (7) **`미채움목록.md` 신설** — 근거가 없어 못 채운 항목을 한자리에 모았다: 거다이맥스 기술 일본어 33(PokeAPI 에 항목 없음·위키에 기술별 문서 없음) · 요정의깃털 설명 2(PokeAPI 항목은 있으나 문구가 빔) · 특성 일본어 설명 3 · 포켓몬위키 `Champions 기술` 문서 16종(Serebii 로 대체 중, 급하지 않음). 각 항목에 왜 못 채웠는지와 점검 명령을 적었다. 되돌리기 = `git reset --hard checkpoint-before-item-effects`. lint 0 · build · verify 통과. 커밋 519865e/4cb4d8c/b5bc07c (포케노바 5892757) |
| 2026-09-10 | **거다이맥스 기술 33종 일본어 이름 + 미사용 파일 정리.** (1) `quiz_moves.json` 의 `ja` 가 영문 슬러그(`g_max_vine_lash`)로 남아 있던 33종을 채웠다. PokeAPI 기술 목록(937개)에 거다이맥스 기술이 아예 없고(맥스 기술 20종만 있다) 포켓몬위키에도 기술별 문서가 없어 그동안 근거가 없었는데, **Bulbapedia** 에 `|jname=` 으로 들어 있었다(거다이편달 = `キョダイベンタツ`). **출처 순서에 Bulbapedia 를 세 번째로 추가** — `PokeAPI > 포켓몬위키 > Bulbapedia`. 앞의 두 곳에 **아예 없을 때만** 보고, 지금 그 대상은 거다이맥스 기술뿐이다(`gmax_move_title` 이 그 외에는 None 을 준다). 리다이렉트로 엉뚱한 문서에 닿을 수 있어 값을 쓰기 전에 그 문서의 영어 이름(`|name=`)이 기대와 같은지 확인한다 — 일부러 어긋나게 넣어 `None` 이 나오는 것을 확인했다. **기술 929개 중 일본어 이름이 빈 곳이 0이 됐다.** 찾는 김에 확인한 것 — 파동의방호·천정부지·불꽃의갈기의 일본어 **이름**은 이미 맞게 들어가 있다(`はどうのぼうご`·`うなぎのぼり`·`ほのおのたてがみ`, Bulbapedia 와 일치). 빠진 건 설명뿐이다. (2) **남은 미채움은 설명 5건** — 요정의깃털 설명 2(PokeAPI 항목은 있으나 문구가 비어 있고 Bulbapedia 에도 설명 절이 없다) · 특성 3종 일본어 설명 3(Bulbapedia 도 영어 설명만). 그 외 포켓몬위키 `Champions 기술` 문서 16종은 Serebii 로 잘 돌아가 급하지 않다. (3) **`pokenova_structure.json` 무시 처리** — 2026-04-06 에 한 번 만든 구조 정리 문서인데 코드·문서 어디에서도 참조하지 않고 값도 낡았다(특성 308 vs 실제 313 · 기술폭 1286 vs 실제 1285). 구조 설명은 `CLAUDE.md` 가 최신이라 파일은 두고 `.gitignore` 에 넣었다. verify · lint · seed 통과. 커밋 48418e8/4c484bc (포케노바 e00b254/17839cb) |
| 2026-09-10 | **도감 카드 팝업에 특성 표시 — 특성이 위, 기술이 아래.** (1) 도감 카드를 누르면 챔피언스 기술만 나왔다. 특성은 그 포켓몬을 고를 때 가장 먼저 보는 정보인데 계산기까지 가야 알 수 있었다. 팝업 맨 위에 **특성**, 그 아래에 **기술**을 둔다. 숨겨진 특성에는 배지를 달고, 설명은 화면 언어에 맞춰 나오며 번역이 비면 한국어로 떨어진다(도구·특성 도감과 같은 규칙). **메가·알트폼은 그 폼의 특성**을 보여준다 — 메가메가니움=메가솔라, 루카리오 메가Z=파동의방호, 시비꼬 옐로 페더=우격다짐(숨겨진 특성). (2) **분류 필터·정렬을 기술 제목 아래로 내렸다.** 기술만 거르는 버튼인데 특성보다 위에 있어서 무엇을 거르는 건지 헷갈렸다. (3) 특성은 도감 목록(`getPokedexEntries`)이 아니라 팝업이 부르는 `/api/data/learnset/[en]` 에 실었다 — 목록은 230종을 한 번에 내려보내는 곳이라 설명까지 실으면 첫 화면이 무거워진다. 팝업은 어차피 거기로 한 번 오니 같이 받는다(응답에 `abilities` 추가, 기존 필드는 그대로라 호환). (4) 파일이 커져 셋으로 갈랐다 — `AbilityList.tsx`(특성) · `MoveTable.tsx`(기술 표) · `PokemonDetailModal.tsx`(껍데기·필터·정렬). 역할이 바뀌어 `MoveListModal` 에서 개명했고, 팝업 aria-label 도 "…의 특성과 챔피언스 기술"로 고쳤다. (5) **실제 화면에서 확인** — Playwright(로컬 chromium)로 이상해꽃(심록·엽록소) · 시비꼬(위협·의욕·근성) · 루카리오(불굴의마음·정신력·정의의마음)를 한국어·영어 둘 다 열어봤다. lint 0 · build 통과. 커밋 2833d40 |
---

## 10. 다국어(i18n) 언어 전환 기능

> **목적:** 해외 유저 접근성 향상 — 브라우저 언어 자동 감지, KO/JA/EN 3개 토글 버튼 제공  
> **상태:** ✅ KO/JA/EN 전 페이지 완료·main 머지(2026-04-19). 번역 미완으로 걸어뒀던 `LANG_LOCKED`도 2026-07-11 해제돼 토글·자동 감지 정상 동작. 남은 것은 **데이터 번역**뿐 — 도구 효과·특성 설명(`desc`)이 아직 ko만 있다  
> **도메인:** https://pochams.com

### 10-1. 구현 방식

**브랜치:** feature/japanese (일본어 개발 중 — main 미머지)

**언어 결정 우선순위:**
1. URL 파라미터 `?lang=en` / `?lang=ko` / `?lang=ja` (공유 링크 최우선)
2. localStorage 저장값 (재방문 시 이전 선택 유지)
3. `navigator.language` 브라우저 언어 감지 (ko → 한국어, ja → 일본어, 나머지 → 영어)

**핵심 파일:**
| 파일 | 역할 |
|---|---|
| `src/contexts/LangContext.tsx` | React Context — `lang`, `setLang`, 감지 로직 |
| `src/lib/i18n.ts` | UI 텍스트 번역 사전 (nav, 버튼, 섹션 제목 등) |
| `src/components/shared/T.tsx` | `<T ko="한국어" en="English" />` 인라인 번역 컴포넌트 |
| `src/components/shared/Header.tsx` | KO / JA / EN 3개 토글 버튼 |
| `src/app/layout.tsx` | LangProvider 래핑, `<html lang>` 동적 반영 |

### 10-2. 데이터 필드 전환

DB에 `ko`/`en`/`ja` 필드가 존재한다. 언어에 따라 전환:

```tsx
const { lang } = useLang()
pokemon[lang]  // 'ko' → "리자몽", 'en' → "Charizard", 'ja' → "リザードン"
```

**샘플 이름 다국어 변환 유틸:** `src/lib/nameMaps.ts` — `buildSampleDisplayName(sampleName, pokemonKo, pokemonEn, lang, pokemonJa?)` 함수로 DB 저장된 한국어 이름을 런타임에 영어/일본어 형식으로 변환.  
- 영어: `"{영어이름} Set"` 형식  
- 일본어: `"{일본어이름} サンプル"` 형식  
- 한국어: DB 저장값 그대로

**일본어 명칭 소스:**
| 분류 | 출처 |
|---|---|
| 포켓몬 이름 | PokeAPI `ja-Hrkt` 필드 (katakana) |
| 기술·도구·특성 | PokeAPI `ja-Hrkt` 필드 |
| 성격 | `nameMaps.ts` NATURE_JA 수동 정의 |
| 타입 | `nameMaps.ts` TYPE_JA 수동 정의 |

### 10-3. 작업 체크리스트

- [x] `LangContext.tsx` — KO/JA/EN 3개 언어 감지·저장
- [x] `i18n.ts` — KO/JA/EN 전체 UI 텍스트 번역 사전
- [x] `Header.tsx` — KO / JA / EN 3개 토글 버튼 (데스크탑·모바일)
- [x] Prisma 스키마에 `ja String @default("")` 추가 (PokemonData, MoveData, ItemData, AbilityData)
- [x] PokeAPI 스크립트로 일본어 명칭 수집·DB 반영
- [x] `nameMaps.ts` — NATURE_JA, TYPE_JA export
- [x] `GameNamesContext.tsx` — translateMove/Item/Ability/Nature/Type 일본어 지원
- [x] `DamageCalc.tsx` — 계산기 전체 일본어화 완료
- [x] `PokedexClient.tsx` / `PokedexPopup.tsx` — 도감 일본어화 완료
- [x] 샘플 이름 일본어 suffix (`buildSampleDisplayName` 확장)
- [x] `PokedexEntry` 타입에 `ja` 필드 추가
- [x] `feature/japanese` → main 머지 완료 (2026-04-19)
- [x] 커스텀 도메인 pochams.com 연결 완료
